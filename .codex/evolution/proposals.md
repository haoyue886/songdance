# 进化建议

> evolution-runner 消化信号产出的改动建议，每条一行 - 开头放 ## 待审阅 区。
> session 启动主 Agent 逐条问你：同意即改对应文档，全盘否定即删 signal 和本条，一半一半按你认可的改。

## 待审阅

- 依据信号：用户指出“借鉴竞品”被缩减为接入同类渲染能力和字面验收，随后 git 历史中 `ffd8937` 只补了高亮、点击定位与导航，`bfff9e2` 又二次修复为固定工作区、禁止自动滚动、任意位置定位、区间选择及播放边界，证明竞品参考在需求翻译阶段被反复丢失真实交互合同；归类：改 Skill；落到 `.agents/skills/product-spec-builder/SKILL.md` 的「搜索增强双遍」；改动摘要：用户以竞品或现有产品描述目标时，不能把“采用同类库/组件”或几个表面功能视为完成，必须把参考体验拆成可观察的布局约束、触发方式、状态变化、滚动/跟随行为和边界规则，并逐项写入需求与 Given-When-Then 验收；只拆用户明确要借鉴的体验，不照抄竞品全部能力。
- 依据信号：专业评审再次指出自动谱面遗漏对外数值格式、边界组合、可用分配和必要语义标记；git 历史中 `f0a96845` 先以可解析和总体人工可读建立门禁，`89321e23`、`3b55bf6`、`b328dad1`、`6eaceaef` 又连续补修弱起/延音、分配、结构及评审产物，证明面向专业使用的结构化成品在需求阶段被“格式合法/平均可用”替代了领域惯例与实际可用性；归类：改 Skill；落到 `.agents/skills/product-spec-builder/references/question-bank.md` 的「7.1 验收标准可测化」；改动摘要：当产品生成可下载或交给专业下游使用的结构化成品时，必须查明目标行业规范和真实使用约束，把对外表示、边界组合、实体分配可用性及必要语义标记拆成代表性样本验收，并让已知专家否决样本单独阻塞，不能用可解析、字段齐全或总体通过率代替专业可用；具体领域术语和数值留在项目需求，不写进通用 Skill。
