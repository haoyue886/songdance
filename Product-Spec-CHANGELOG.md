# 变更记录

## [v1.40] - 2026-09-15

### 修改

- 明确 AC-113：`14-hand-crossing` 只统一 60 BPM 八分时值，并保留模型给出的交汇同音；不得用参考音高模板替换或补写未检出的音。合成源在 C4 有两个 MIDI 起音，老师记谱左手可空拍，冲突时保留原始事件与审计，不改写旧生成器。
- 本阶段不生成最终评审包，等待其余批次评审到齐后统一重建。


## [v1.39] - 2026-09-06

### 新增

- 记录钢琴老师对 06–10 分批评审的原文、样本真值冲突、本地产物指纹和未决状态，新增 AC-104–AC-108。
- 明确专业反馈必须绑定可核对的音频/参考 MIDI 与版本；泛音删除、BPM/音符单位等价记谱、单/双谱表和 88 键覆盖均需分层证据，不得凭总数或单个退化样本直接改模型。

### 修改

- 06 的现有合成样本保留为失败对照：生成器实际含四音和持续重叠，与“每和弦三个基频音”的反馈存在合同冲突，待新版本真值确认后再修。
- 09 的一拍一音与五声音阶反馈作为高价值可读性基线；08 的八分音符小节与当前 BPM 需保持绝对播放时间等价，不直接降速。
- 10 的低音漏检先按输入带宽退化、原始模型事件和后处理分层评估，不直接启动重训或替换模型。
- 07-soft 本批确认只是复制旧评审记录，不作为新缺陷重复修复；历史 baseline 与 `minor_edits` 签收继续保留。
- 06 三音候选已按老师提供的 C 大调级数确认：`5-1-3 = G3-C4-E4`、`6-1-4 = A3-C4-F4`、`5-7-2 = G3-B3-D4`；独立于旧四音 legacy 样本，不覆盖旧真值。
- 06 候选审计改为完整核算 45 个真值匹配与 34 个额外事件，并逐事件保存默认/实验策略决策；0.60 速度比实验因误删真实低音八度明确禁止进入生产。
- 为 10-device 增加全频/带限与标准/弱低音的 2×2 隔离实验，要求低音和右手分开报告，未完成配对证据前不把单个 C2 漏音解释为模型需要重训。
- 记录 11-waltz-34 的专业参考谱反馈，新增 AC-109–AC-111：6/8 复合拍分组、左右手时值守恒和检测 BPM/记谱 BPM 分离；旧 3/4 样本保留为对照。
- 10-device 配对实验修正为共享 stem/固定增益并加入频谱、清洗和谱面分配证据；结果只支持“带限与弱低音交互”，不支持弱音独立效应或更换 Basic Pitch。记谱层额外低音对齐损失另立修复项。

## [v1.38] - 2026-08-31

### 修改

- 记录 `07-soft` 修复后的专业人工复评通过，评级为 `minor_edits`，解除其对 Phase 36 的阻塞；原始 `needs_redo` 缺陷、评审 PDF 和修复前后指纹继续保留。
- AC-101–AC-103 标记为已通过：完整首拍弱起反证、无 F# 时的 C 大调保护、残响与独立音符时值隔离均通过人工复评和自动回归。

## [v1.37] - 2026-08-28

### 新增

- 记录最终离线评审包 `songdance-structure-review-2065d538e23d.zip` 中 `07-soft` 的专业复评为 `needs_redo`，并绑定评审 PDF、包内 `07-soft` MusicXML/WAV 指纹和三项缺陷：误判弱起造成小节线整体前移、无 F♯ 证据却写入一升号调号、钢琴残响被写成二分/附点等延长时值。
- 新增 AC-101–AC-103：四分音符型完整首小节弱起反证、调号导音缺失反证和残响不改变独立起音时值门禁。

### 修改

- 明确 `07-soft` 继续作为失败对照，不得用 16 段总体通过数掩盖；修复后必须重新生成该样本并重新人工评审。
- 评审证据显示 PDF 当前产物仍是双谱表，但低音谱表为空；在本次修复中先保证时间线、调号和时值正确，单谱表语义按 Phase 34 独立门禁处理，不把三个问题混成一次不可归因改动。

## [v1.36] - 2026-08-28

### 修改

- 关闭“单次 `V–I/i` 是否足以写调号”的歧义：孤立属到主只提高候选权重；自动写调号必须有位于乐句边界的完整终止式，并由同一完整乐句中的另一类独立证据支持。一次 `V/x→x` 视为临时主音化，新主音延续到后续完整乐句或获得第二个终止证据后才可升级为稳定局部调性。
- 小调首版统一输出“主音 + minor”；自然、和声、旋律小调视为同一调性中的音阶形态。常规小调调号保持不变，升高的第六/第七级使用临时升降号，不因音阶形态切换调号。
- 将“低音谱表长期为空”改为语义门禁而不是空小节比例：只有整段为可用高音谱表清楚阅读的功能性单旋律时才输出单高音谱表；任一完整乐句存在真实独立低声部或左手伴奏，就全段保留大谱表且不中途改变谱表数量。
- 首个非西洋模式版本只对外显示 `Chinese pentatonic`、`Japanese miyakobushi`、`Blues` 家族；宫/商/角/徵/羽与布鲁斯大小调变体保持 `unknown`。主音至少需要乐句终止/停驻、结构重音回归、稳定低音或持续音中的两类证据，禁止用最高频音或最后音直接决定。
- 新增 AC-099 与 AC-100，并扩展 AC-096–AC-098、局部调性数据合同和固定集覆盖。

### 限制

- 细分五声、布鲁斯变体以及更多地区调式仍须独立专业固定集，不纳入当前生产标签。

## [v1.35] - 2026-08-28

### 修改

- 根据钢琴老师专业访谈，将片段分析目标收紧为“局部调性”而不是无法证明的整曲原调号；终止式与和弦进行升为主证据，导音、特征音、实际音级、低音运动和结尾位置只作综合辅助，禁止仅凭结尾音或全段色度直方图确定调性。
- 明确推断调性与原谱调号继续分开：稳定局部调性达到固定集阈值时可用 `inferred_local_tonality` 调号减少临时变音，但必须标成片段排版推断；证据不足时不写确定调号、保留临时升降号，并最多显示两个候选及“请根据完整音频确认主音”。
- 将“五声音集合”与“五声调式旋律句法”拆开，新增 `1-3-5-5-6-5 / 3-1-5-5-5-3-1` 的显著 `1→5` 跳进反例，禁止只因出现 `do/re/mi/sol/la` 就判为中国五声调式。
- 新增单谱表可读性规则：整段只有单一高音旋律且没有独立低音/左手织体时使用单高音谱表；存在真实低音或左手伴奏时仍保留同一 Piano part 内的高低音大谱表。
- 新增 AC-095–AC-098 和至少 20 段的独立局部调性固定集，要求用参考谱与独立音乐人工复核建立真值，统计 top-1/top-2、候选校准、确定调号误写率和保守不写率；禁止让被测算法生成自身真值。

### 限制

- 中国五声、日本都节和布鲁斯的具体标签粒度尚未由专业样本确认，先作为 P1 研究子集；印度、埃及等少见体系不进入当前生产承诺。

## [v1.34] - 2026-08-22

### 修改

- 记录 Petzold BWV Anh.114 人工评审为 `needs_redo`：未识别装饰音导致短音占用正拍并引发节奏偏移，低音自然泛音被误写为独立高音；候选状态改为 `candidate_rejected`，不得继续作为初级公开示例。
- 新增装饰音门禁：只有高置信证据才能生成不占小节时值的 MusicXML grace note，不能把普通短时值音符机械改成装饰音；证据不足时以 `ORNAMENT_REVIEW_REQUIRED` 阻塞公开示例。
- 收紧泛音抑制：低音基频优先规则必须结合起音、音频能量和相对速度证据，保留审计记录并保护独立八度/和弦；音频证据不可用时不允许仅凭音程删除。
- 下一初级候选改为优先核验哈农公式化练习，仍须同时满足真人演奏、明确授权、参考谱和人工评级门禁，不能用 MIDI 合成代替真人评测。
- 固定下一候选为 Freesound `522868` 的哈农 No.1 `piano7_ex1_good_good.wav`：单条页面标记 CC0，Zenodo `10.5281/zenodo.3898631` 提供 CC BY 4.0 的完整 WAV、起音/色度标注和 LilyPond 真值，参考谱采用 IMSLP/Mutopia 可下载版本；后续入库仍须绑定具体文件哈希并经过实际转录与人工评级。
- 修复泛音保护对 `04-arpeggios` 的连带回归：`simple-arpeggio-filter-v6` 只在至少 3 个踏板支持的稳定重复周期与同一音符的起音/STFT 泛音对证据同时成立时删除假高音，禁止跨周期复用同 velocity 证据；无音频证据时保留，并保护强真实八度、带独立攻击的弱同时八度/和弦、弱但有独立起音的跨手音、单槽和弦、稳定平行声部与持续独立声部。实际产物恢复为 119 个记谱音和既定八音循环。
- 修正同时起音保护不能覆盖真实弱十二度及通用清理路径的问题：不再按二次谐波阶数硬编码真实音保护，通用 `extract→clean` 与简单琶音过滤共用同一独立攻击判据；当候选高音明显早于低音结束时，使用候选结束后的短窗频带能量判断高音是否真实释放。尾音比扣除起音前稳定噪声底，相邻同音占用探测窗时标为不可判定；高频随候选结束明显下降时保留独立音，高频继续随低音存在时才允许按自然泛音删除，尾音不可测时保守保留。
- Playwright E2E 的 Next 开发服务改用当前运行独有的 `.next/e2e-*` 构建目录与 `.next/e2e-config` 临时 tsconfig，并在 teardown 无条件清理，允许测试与用户正在运行的项目服务并存，不再争用 `.next` 锁或改写正式 `tsconfig.json`。

## [v1.33] - 2026-08-21

### 新增

- 用户确认将 Christian Petzold《G大调小步舞曲》BWV Anh.114 作为 Task 32.2 的初级候选；候选使用 Wikimedia Commons 上 KasraR 的真人数字钢琴演奏（CC BY-SA 3.0，并附公有领域声明），参考谱采用 Pianovera 页面提供的 Mutopia 公版钢琴 MIDI 与 PDF。
- 候选固定条件为 G 大调、3/4 拍、32 小节、两声部、最短四分之一拍；在当前流水线产物完成人工评级至少 `minor_edits` 前，状态保持 `candidate_pending`，不替换 K.545 失败对照、不关闭其复评任务。

## [v1.32] - 2026-08-21

### 修改

- 将 K.545 人工复评标记为暂时暂停：保留 `pending + needs_redo`、失败对照页面、原音、谱面和下载产物，不把暂停视为通过，也不解除 `needs_redo` 对可用示例发布的阻塞。
- 明确恢复条件：后续重新启动人工复评时，仍须按参考谱完成评审并达到至少 `minor_edits`，才能进入可用示例准入判断。

## [v1.31] - 2026-08-20

### 修改

- 将用户下载的 IMSLP 公版 K.545 完整 MIDI 固定到仓库参考 fixture，记录来源 URL、许可、SHA-256、73 小节、4/4、C 大调和 17 个公开产物小节的有序参考映射。
- 发布门禁改为离线解析固定 MIDI，并对参考文件、来源指纹、完整小节覆盖和音频映射做 fail-closed 校验；不再把 music21 仅覆盖前 12 小节的 corpus 当作完整参考真值。

## [v1.30] - 2026-08-19

### 修改

- 记录网站当前 `03-mozart-sonata` 示例的专业人工评审为 `needs_redo`：当前 MusicXML 错把参考谱的 C 大调记谱调号写成 G 大调，高音谱表承担 276 个音而低音谱表仅 75 个音，低音织体缺失且谱面休止符/声部结构混乱。
- 收紧 SCOPE-012 与新增 AC-085–AC-090：失败的新评审覆盖历史 `minor_edits`；当前示例保留为明确标记的失败对照并继续修复，不得冒充可用，也不得靠换简单曲目关闭 K.545 缺陷。通过评级的默认示例仍优先选择带真实演奏、授权和参考谱的哈农/车尔尼/拜厄或同等难度素材。
- 将局部调性估计与原谱记谱调号拆开保存；带参考谱示例按参考谱校验调号，普通上传只能显示推断调性，不能声称恢复整部作品的原始调号。
- 为十六分、三十二分、六十四分音型建立独立真值门禁。music21 本地 K.545 参考谱证明本次快速音型为十六分音符，因此不采纳“全部改成三十二分”的口头建议，只保留“快速音型可能漏音”作为待量化验证的问题。
- 将保持音高的 0.5 倍速推理限定为离线 A/B 候选：时间缩回原轴后必须证明召回提升且 precision、人工可读性和时延不退化，未经固定集验证不得进入生产。
- 新增双谱表密度、低音域覆盖和织体连续性校验；发现低音谱表近乎为空时标记 `STAFF_DISTRIBUTION_SUSPECT` 并阻塞公开示例，禁止只按全曲音符比例机械搬移。
- 首轮修复产物已重置为 `pending`，并显式保留最近完成的 `needs_redo` 历史：原始模型音符数仍为 321；未知手分配由 197 降至 18，左手事件由 25 增至 107，staff 分布门禁通过；MusicXML 改为 C 大调、无假弱起，快速音型保留十六分音符。该数据只证明已修结构项，不代表人工复评通过。
- 将 K.545 发布真值绑定到 music21 随锁定依赖提供的参考谱标识、文件 SHA-256、逐小节最短时值统计和当前固定音频指纹；发布器仅接受参考谱 C 大调与十六分量化合同，错误 G 大调、32 分量化、staff 未评估或字段缺失均 fail-closed，并把参考验证摘要写入公开 provenance。
- 普通上传的自动调性分析在结果页显式标为“推断调性”；触发 `STAFF_DISTRIBUTION_SUSPECT` 时显示左右手谱表分配存疑警告，引导结合钢琴卷帘、MIDI 和原音校对，不再只把风险藏在 JSON 中。

### 限制

- Wikimedia Commons 候选检索因当前网络审批服务不可用尚未完成；在授权、真实录音与参考谱三项齐全前，不指定新的公开示例文件，也不以合成固定集代替。
- music21 锁定依赖内的 K.545 参考谱当前只覆盖第 1–12 小节，尚未证明固定 25–55 秒音频与整段 17 小节转录结果的逐小节对应；现有门禁可阻止调号、最短时值及其语义副本被篡改，但在补齐完整参考谱和片段映射前，K.545 必须继续保持 `pending / needs_redo`，不得标为可用。

## [v1.29] - 2026-08-19

### 修改

- 修复公开示例未随当前生产后处理产物发布的问题，继续以固定发布源、当前版本与文件指纹作为一致性门禁。
- 新增 AC-084：本地上传生成的谱面使用去扩展名后的原文件名作为唯一可见标题；空标题或旧任务使用 `SongDance Transcription` 回退。
- MusicXML 不再把内部 `Music21` 引擎显示为作曲者，也不重复显示 work title 与 movement title；内部 `<software>` 版本元数据继续保留用于溯源。
- 示例页直接展示当前产物复评状态、最近一次已完成人工评级和真实授权来源，不再把 Wikimedia Commons 来源误标为 Musopen。
- 修复首小节为合法弱起时 OSMD 时间映射被误判失效的问题，统一校验、点击定位和选区几何的弱起时间偏移。

## [v1.28] - 2026-08-19

### 修改

- 记录 `04-arpeggios` 第四次专业人工评审：谱面 BPM 小数位过多，左手休止符跨拍组合不规范，且缺少 `mp` 力度记号。
- 修订 AC-072 与 AC-082：将 `C4` 从低音谱表移到高音谱表，完整小节改为左手 `C3-G3`、右手 `C4-E4-G4-C5-G4-E4`，左手休止符必须按整拍边界组合。
- 新增 AC-083：内部节拍分析保留原始精度，对外谱面、MIDI 与主时间线 BPM 四舍五入为整数；`04-arpeggios` 起始处输出 `mp`。
- 收紧 AC-083 的力度证据：仅为中位 velocity 不高于 96 的稳定琶音输出 `mp`，强奏同音型不得误标；新增 `dynamic-marking-v1` 版本隔离旧缓存。
- 分手算法升级为 `voicing-v3`，踏板衰减过滤升级为 `simple-arpeggio-filter-v3`，防止新旧手分配规则共享错误缓存与质量指纹。

## [v1.27] - 2026-08-18

### 修改

- 记录 `04-arpeggios` 第二轮专业人工复评仍为 `needs_redo`：PDF 证明左右手被分别声明成 Piano part，Sibelius 将每个 part 展开为大谱表，最终形成两组大谱表共四行；踏板衰减还被误记为同音重复起音。
- 修正 AC-078 的错误输出合同：标准钢琴 MusicXML 必须是一个 Piano part 内的高音、低音两个 staff，而不是两个 Piano part。
- 新增 AC-082，把完整小节目标记谱钉死为左手 `C3-G3-C4` 与右手 `E4-G4-C5-G4-E4` 的八分音符槽位；只过滤稳定目标音型中出现在错误槽位的踏板衰减重识别，并保护独立持续低音、内声部及非目标琶音。

## [v1.26] - 2026-08-18

### 修改

- 记录专业人工复评对 `04-arpeggios` 的 `needs_redo` 结论：两个钢琴 part 被错误展开为四组谱表，第二小节起八分音符循环逐小节提前四分之一拍，延音被误判为多声部并堆积休止符，且缺少真实踏板记号。
- 将修复拆成四项可验收门禁：标准钢琴输出最多两行（高音+低音）；4/4 八分音符循环按首小节基准重建；同音延音优先合并到同一声部并抑制伪声部休止符；只有连续密集起音均有未释放尾部能量的音频级证据或 CC64 证据时写入踏板记号，并移除替代性长连线。
- 专业复评未通过前，`04-arpeggios` 继续保持 `needs_redo`，不能用整体可读数量掩盖单样本失败。

## [v1.25] - 2026-08-17

### 新增

- 扩展 REQ-013 国际化范围：在英语和简体中文之外加入日语、韩语、西班牙语、巴西葡萄牙语、法语和德语。
- 新增 AC-075–AC-077，要求八种语言的服务端入口文案、物理消息基线、语言切换、canonical、hreflang 和 sitemap 保持一致。
- 明确首批新增语言的交付边界：入口核心文案先本地化，其余 namespace 使用独立英语基线文件；后续按 namespace 翻译审校，不通过运行时缺 key 静默回退。

### 假设

- 首批新增语言按海外音乐制作人覆盖面选择；语言优先级可在上线数据和翻译质量评审后调整，不改变共享页面组件和 API 错误码合同。

## [v1.24] - 2026-08-17

### 修改

- 简化 AC-074 离线评审交付：评审者只需打开本地包播放 16 段、选择评级并截图回传，不再在包内声明 MIDI/DAW 经验或提交 `completed-review.json`；正式评级由项目维护者根据截图录入。

## [v1.23] - 2026-08-13

### 新增

- 新增 AC-070–AC-074，将专业评审指出的 `04-arpeggios` 假弱起、全曲小节线错位、延音伪多声部、左右手错配和泛音误音拆成独立可测门禁。
- 新增弱起反证机制：以首段小节时值完整性、起音/重音、beat/downbeat 相位和后续乐句周期共同决策；完整 4/4 八分音符循环必须否决弱起。
- 新增音频感知的延音与泛音证据要求；禁止仅凭音符重叠伪造踏板，或仅凭八度关系删除音符。

### 修改

- 将结构质量门禁改为同时检查总体可读数和已知 `needs_redo` 样本；专业失败项未逐条修复并复评前，不得用其他通过样本掩盖。
- 简单非交叉手琶音采用稳定音区策略作为排版规则，并继续保留交叉手场景的置信度与 `unknown` 语义。

## [v1.22] - 2026-08-12

### 修改

- 固定 Basic Pitch 为钢琴生产模型，取消 AMT-APC、PiCoGen、Aria-AMT、Omnizart 等专用钢琴模型的接入、固定集 A/B 与人工盲评投入。
- 将 SCOPE-017 与 AC-028 改为 Basic Pitch 谱面可读性优化门禁：原始模型事件保持不变，本轮先让结构分析覆盖最长 90 秒输入并正确生成双手共享边界的 MusicXML 弱起。
- 半/双倍 BPM、自适应/三连音量化、宽容和弦聚类和声部压缩不纳入本轮生产改动；它们必须分别用固定集证明没有召回或可读性退化后再上线。
- 要求优化后弱起不产生人为整小节空白、快速复调召回下降不超过 5%、至少 13/16 结构样本人工可读，并继续通过 music21、OSMD 和独立 XML 解析。

## [v1.21] - 2026-08-11

### 新增

- 新增 SCOPE-024、TASK-011、REQ-013 与 AC-064–AC-069：网站默认英语，使用同一套页面组件和消息目录支持简体中文；英语采用无前缀 URL，中文采用 `/zh` 前缀，并要求服务端翻译、语言切换、错误码本地化和双向 hreflang。
- 新增 `next-intl` 4.x 国际化依赖合同；API 保持稳定结构化状态和错误码，UI 文案由 Web 根据 locale 统一映射。

### 修改

- 将 `audio to MIDI converter` 核心搜索意图和原 `/audio-to-midi` 内容合并到默认英语首页 `/`，旧地址改为永久重定向，不再维护重复落地页。
- 调整 REQ-012 与 AC-062：英语首页成为核心 SEO 页面，语言版本使用自引用 canonical、互相 hreflang 和英语 `x-default`。

## [v1.20] - 2026-08-11

### 新增

- 新增 SCOPE-023、TASK-010、REQ-012 与 AC-059–AC-063，建立公开页面技术 SEO、英语核心工具页、临时任务 `noindex`、sitemap、canonical 和真实结构化数据合同。
- 新增 `SEO-PLAN.md`，记录关键词假设、首批 10 个页面资产、3 个页面 Brief、发布清单和 30 天 GSC 迭代方案。

### 限制

- 没有真实关键词工具或 GSC 数据时不得伪造搜索量/KD；首批页面出现索引、曝光或转化信号前，不建设批量格式页、乐器页或无证据比较页。

## [v1.19] - 2026-08-11

### 新增

- 正式谱面选区存在时，双击谱面工作区可直接清除选区并恢复整曲播放边界；双击不移动播放头或自动滚动。

### 修改

- 为消除浏览器双击阈值不可读取造成的定位竞态，正式选区存在时单击谱面或选区边界不再定位，只有双击清除选区；无选区时继续沿用即时的普通单击定位。

## [v1.18] - 2026-08-11

### 新增

- 为已提交谱面选区的起止边界增加 16 CSS px 透明命中带，hover 使用系统原生 `ew-resize` 双向箭头（`↔`）。
- 支持基于原选区拖动任一端点二次调整，沿用实时 draft、事件磁吸、跨系统/页、边缘滚动与取消恢复合同；松手前不修改正式播放边界。

### 修改

- 原“本轮不提供独立端点调整”非目标改为“不显示常驻手柄、不支持整体平移”，边界调整只通过 hover 命中带出现。

## [v1.17] - 2026-08-10

### 修改

- 将谱面区间选择从“松手后生成”升级为拖动中的实时临时选区，要求边界、遮罩和选区内容在下一动画帧内更新。
- 增加反向拖动、跨小节/系统/页面连续选择、连续时间映射与事件磁吸，以及靠近谱面视口边缘时的内部自动滚动。
- 明确临时选区与播放边界分离：仅释放指针后提交，取消或失去捕获时恢复旧选区；本轮不实现选区整体平移和双端手柄。

## [v1.16] - 2026-08-09

### 修改

- 补充谱面区间拖选的鼠标反馈：可选择时显示十字光标，按下拖动时切换为抓取光标，释放或取消后恢复。

## [v1.15] - 2026-08-08

### 修改

- 将结果页从“曲谱跟随播放自动滚动”改为“固定播放器 + 独立滚动谱面视口”，播放时只更新当前小节高亮。
- 将五线谱交互从整小节点击定位扩展为可映射位置点击和任意谱面区间选择。
- 明确选区播放边界：从选区起点开始，到终点停止或在选区内循环；映射失败时只禁用谱面交互，不阻塞播放和下载。

## [v1.14] - 2026-08-08

### 新增

- 在结果页五线谱预览中新增当前小节高亮、点击小节定位和上一/下一小节控制，并要求首尾边界禁用。
- 新增小节映射降级与窄屏验收：映射不可用时保留五线谱、播放和下载，375 px 下导航不得造成横向滚动。

## [v1.13] - 2026-08-07

### 修改

- 内置公共示例从人工评级为 `needs_redo` 的 Bach 快速复调片段改为真实莫扎特 C 大调奏鸣曲片段；拒绝用困难失败样本或合成标准答案作为默认体验。
- 公共示例新增质量准入：最近一次人工评级至少为“少量修改”，拍号必须由结构分析实际识别，并以文件指纹保证公开音频和三项转录产物与固定发布源一致。

## [v1.12] - 2026-08-06

### 修改

- 专用钢琴模型替换暂缓为 P1：当前三个候选均未通过生产许可前置门禁，继续使用 Basic Pitch，不下载候选权重、不投入 GPU、固定集 A/B 或人工盲评。
- Phase 12 的审计记录和许可门禁继续保留；没有许可合格候选不再阻塞确定性音符清洗、结构分析和谱面重建，开发顺序直接进入 Phase 13。
- Phase 13 不再仅凭短间隔自动合并相邻同音：缺少独立重触键证据时保留不同起音并记录原因，避免把短促重复音误合并；异常超长延音改为可配置裁剪。

## [v1.11] - 2026-08-06

### 修改

- 模型选型改为许可前置门禁：代码、权重或训练数据不允许目标生产用途的候选，在投入 GPU、固定集 A/B 和人工盲评前直接淘汰出生产候选，只允许保留为 `research_only` 审计参考。
- Aria-AMT 的固定权重为 `CC-BY-NC-SA-4.0`，状态改为 `research_only`，取消后续 CUDA 推理和固定集 A/B；线上继续使用 Basic Pitch，Phase 12 只对生产许可先通过的候选投入质量评测。

## [v1.10] - 2026-08-06

### 修改

- Phase 12 隔离评测清单固定 Aria-AMT 代码提交 `a1ab73fc901d1759ec3bc173c146b3c6a3040261`，并要求在评测 manifest 中锁定 `piano-medium-double-1.0.safetensors` 的权重 revision、SHA-256、来源和许可证明；这些信息未验证前保持 `not_evaluated`，不进入灰度。

## [v1.9] - 2026-08-06

### 新增

- 将 Aria-AMT（`EleutherAI/aria-amt`）纳入专用钢琴转录候选。代码为 Apache-2.0 且要求 Python 3.11；模型权重许可、固定 revision、资源成本和固定集 A/B 仍是进入灰度前的强制门禁。

## [v1.8] - 2026-08-05

### 修复

- 明确钢琴“转录演奏”必须使用真实钢琴采样，不得用通用振荡器合成音冒充钢琴；采样加载失败时允许明确提示后的基础合成音降级。
- 将用户反馈的重叠音符、碎片休止符和谱面混乱纳入既有 REQ-009 验收，先落实同起音 Chord、合法 Voice 与 MusicXML 结构校验，不提前宣称 Phase 15 完成。

## [v1.7] - 2026-08-05

### 新增

- 新增 SCOPE-018–SCOPE-022、TASK-007–TASK-009、FLOW-004–FLOW-005、REQ-010–REQ-011 和 AC-029–AC-042，将产品路线扩展为“钢琴优先、单乐器先行、完整混音后置”的多乐器转录平台。
- 新增贝斯、小提琴、长笛、降 B 小号、中音/次中音萨克斯、吉他、鼓和人声旋律的输入与输出合同。
- 新增 `InstrumentProfile`、`TranscriptionTrack` 和 `StemAsset` 数据边界，以及逐乐器固定集、轨道级部分成功、移调乐器和级联删除验收。

### 修改

- 有音高乐器首批统一输出 MIDI 与对应五线谱；吉他/贝斯 TAB、Guitar Pro、人声歌词和歌词对齐明确后置。
- 发布顺序固定为：钢琴质量 → 单音家族 → 吉他 → 鼓 → 完整混音。
- Demucs 仅作为常见 stem 的候选分离器，不能宣称从 `other` 可靠拆出小提琴、长笛、小号或萨克斯；完整混音同时评估 MT3 类直接多乐器 AMT。

## [v1.6] - 2026-08-05

### 新增

- 新增 REQ-009“音乐理解与可读谱面重建”，把原始 MIDI、音符清洗、节拍/调性分析、和弦/多声部重建和 MusicXML 质量校验拆成独立能力。
- 新增 FLOW-003 转录质量复盘、AC-021–AC-028，以及原始/清洗产物、质量摘要和后处理版本的数据追踪要求。
- 新增 SCOPE-017 与 AC-028：Basic Pitch 保持可回滚基线，专用钢琴模型和 Omnizart 只在隔离环境完成质量、许可、时延与内存 A/B 后才允许灰度替换。
- 将 librosa 作为节拍与调性分析实验基线；madmom/Essentia/和弦模型列为对比候选；Demucs 列为受 feature flag 控制的 P1 混音分离实验。
- 将结构回归集固定为至少 16 段并要求保存人工真值；“谱面可读”门槛统一为至少 80%（首版 13/16）。

### 修改

- 明确当前项目仍以“钢琴主体音频”为首版范围，不能把分轨能力或第三方网站能力包装成已支持功能。
- 明确五线谱质量门禁：同一 voice 不得非法重叠，复音必须组织为和弦或合法多声部，低置信度结果必须可解释并保留原始证据。
- 根据官方仓库与当前锁文件核验依赖：librosa 0.11.0 已被 Basic Pitch 间接锁定；Demucs 上游已归档；madmom 模型数据和 Essentia AGPL 许可须在生产接入前审计。

## [v1.5] - 2026-08-05

### 修改

- 钢琴卷帘改为 Canvas 下落视图：固定击键线、底部钢琴键盘和左右手颜色与播放时间线同步。

## [v1.4] - 2026-08-05

### 修复

- 本地开发 CORS 默认允许 Next.js 常用的 `localhost:3000` 和端口冲突后的 `localhost:3001`，避免前端误报无法连接转录服务。

## [v1.3] - 2026-08-05

### 修改

- 将结果页播放来源从技术名词“MIDI”改为“转录演奏”，明确它用于和原始录音对比。

## [v1.2] - 2026-08-05

### 修改

- 将转录质量修复列为 P0：禁止未经固定真实钢琴集验证的高检测阈值造成系统性漏音。
- 要求模型版本记录检测阈值配置，并在模型、阈值或后处理变更后保存原始 MIDI 音符数、人工评审和结果摘要。
- 明确 4/4 拍号与中央 C 左右手分配是基础排版默认值，不得表述为对原曲结构的可靠识别。
- 新增快速复调 Bach 基准的参数 A/B 验收项。

## [v1.1] - 2026-07-31

### 新增

- 新增部署交付边界，区分产品公网目标、部署就绪交付和用户执行的生产验收。

### 修改

- 固定 Vercel 承载 Next.js Web，Railway 或等价容器平台承载 FastAPI、FFmpeg、RQ Worker 和清理任务。
- 将当前交付调整为提供部署方法和配置，不代用户创建云资源、产生费用或声称线上验收通过。

---

## [v1.0] - 2026-07-28

- 初始版本。

- 2026-09-14：新增 AC-112，固定 13-key-change 六音循环使用 3/4 与单旋律六槽位记谱，禁止泛音残留形成和弦。
- 2026-09-14：新增 AC-113，固定 14-hand-crossing 的 60 BPM 镜像八分音符节奏与真实同音交汇保留规则。
- 2026-09-15：AC-113 续作明确区分源 MIDI 同刻同键事件与音频模型可辨识的事件，补齐同音保留整链与落盘时值互校；仅节奏通过，保留候选的音高待复评状态和统一评审包暂缓约束。
- 2026-09-15：新增 AC-114，交叉练习的音尾再次检出需满足原始起音、连续衰减振幅/相位、唯一同音前驱及根起音的联合证据；保留弱重击与同刻同音，参考 MIDI 仅用于结果评估。
- 2026-09-15：明确 AC-113，禁止音高模板替换，保留模型交汇同音与原始审计。
