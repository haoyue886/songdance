# 产品需求规范：SongDance AI Music Transcriber

## 0. AI 使用说明

- 本文档是产品功能、范围、行为和验收标准的单一事实来源。
- AI MUST 优先实现 P0，并按验收标准判断是否完成。
- AI MUST NOT 实现“不在本版本范围”中明确排除的内容。
- 未被用户明确确认的内容必须保留为假设，不得伪装成已验证需求。
- 本产品当前是“假设验证型 MVP”，不是已完成用户验证的商业产品。

## 1. 产品上下文

### 1.1 产品摘要

SongDance 是一个钢琴优先、逐步扩展到多乐器的公开音频转录 Web 应用。当前 P0 先把钢琴转录质量做稳；后续 P1 支持吉他、贝斯、小提琴、长笛、降 B 调小号、中音/次中音萨克斯、鼓和人声旋律，并在单乐器模式通过后增加完整混音的分轨与多轨转录。

有音高乐器输出 MIDI、MusicXML、五线谱预览和 PDF；鼓输出 General MIDI 打击乐轨与打击乐谱；人声只输出旋律音符，不做歌词识别。首版使用成熟开源模型验证核心价值，不承诺达到商业竞品 Songscription 的转录准确率。

### 1.2 用户问题

独立音乐制作人拿到只有音频、没有工程文件的乐器或人声片段时，需要依靠手工扒谱、DAW 内逐音录入、分轨软件和多个转换工具，才能获得可编辑的 MIDI。该流程耗时，且容易出现音高、节奏、复音、乐器归属和分轨伪影问题。

当前没有来自真实目标用户的访谈记录或现状成本数据。以上问题来自竞品研究，属于待验证假设。

### 1.3 目标用户

| 用户类型 | 描述 | 核心需求 |
|---|---|---|
| 主用户 | 需要把乐器或人声录音导入 DAW 的独立音乐制作人 | 快速获得按乐器组织、可继续编辑的 MIDI |
| 次用户 | 希望查看演奏对应音符的学习者 | 查看适合目标乐器的乐谱和时间线 |

音乐教师不是首版目标用户，不实现教学、批注、作业或班级管理。

### 1.4 核心价值

让制作人把一段单乐器录音或完整混音快速变成可编辑、可播放、可导出的乐器轨 MIDI 和基础乐谱，而不是从零手工扒谱和手动分轨。

### 1.5 核心 Job

当制作人拿到一段只有音频的演奏时，帮助他获得可在 DAW 中继续编辑、按目标乐器组织的音符数据。

### 1.6 成功标准

| 判断标准 | 目标 / 信号 |
|---|---|
| 核心质量 | 在固定的 10 段、每段 30 秒独奏钢琴测试集中，至少 7 段 MIDI 被评为“可直接使用或只需少量修改”；快速复调基准不得因过高检测阈值出现系统性漏音 |
| 多乐器质量 | 每个新增乐器必须有独立的至少 10 段固定集，其中至少 7 段被评为“可直接使用或只需少量修改”；一个乐器过门禁不能代表其他乐器也过门禁 |
| 混音质量 | 完整混音只对已通过分轨与转录双重门禁的轨道宣称支持；未知或混在 `other` 的乐器不得被强行贴标签 |
| 端到端完成 | 用户可从公开网页完成上传、截取、转录、预览和 MIDI 下载，无需注册 |
| 任务完成率 | 合法且受支持的输入中，至少 90% 能在一次提交后得到结果或明确可恢复的错误 |
| 处理时延 | 30 秒音频在正常服务条件下 P95 不超过 180 秒；90 秒音频 P95 不超过 360 秒 |
| 验证信号 | 记录上传开始、提交转录、转录成功、播放、下载各格式和失败原因，不以注册量作为北极星指标 |

### 1.7 部署交付边界

- 生产架构固定为 Vercel 托管 Next.js Web，容器平台托管 FastAPI API、RQ Worker 和清理任务，并连接 PostgreSQL、Redis 与私有 S3 兼容存储。
- Vercel 只承载 Web。Basic Pitch、FFmpeg、常驻 Worker 和定时清理不得迁入 Vercel Functions，以免把长任务误做成无状态请求。
- 当前开发交付包含可直接使用的部署声明、生产环境变量契约、部署/回滚/排障手册和本地生产构建证据。
- 实际创建云资源、绑定账号、产生费用和执行生产 URL 验收由用户按手册完成；未实际执行的线上测试不得标记为已验证。

## 2. 范围

### 2.1 本版本范围

| 编号 | 内容 | 优先级 | 备注 |
|---|---|---|---|
| SCOPE-001 | 公开访问的响应式 Web 应用 | P0 | 无需注册即可完成核心流程 |
| SCOPE-002 | 本地钢琴音频上传 | P0 | MP3、WAV、M4A，单文件不超过 25 MB |
| SCOPE-003 | 波形预览和片段截取 | P0 | 默认前 30 秒，最长 90 秒 |
| SCOPE-004 | 钢琴 Audio-to-MIDI 转录 | P0 | 使用成熟开源模型，不训练自有模型 |
| SCOPE-005 | MIDI 后处理和 MusicXML 生成 | P0 | 包含基础量化、小节、拍号及左右手分配 |
| SCOPE-006 | 五线谱和钢琴卷帘预览 | P0 | 可播放、暂停、循环、变速和整体转调 |
| SCOPE-007 | MIDI、MusicXML、PDF 下载 | P0 | PDF 来自当前五线谱渲染结果 |
| SCOPE-008 | 异步任务进度、失败提示和重试 | P0 | 页面刷新后可凭任务 ID 恢复 |
| SCOPE-009 | 匿名限流、临时存储和自动删除 | P0 | 防滥用并保护上传内容 |
| SCOPE-010 | 基础产品埋点 | P0 | 不采集原始音频内容作为分析数据 |
| SCOPE-011 | YouTube 链接导入 | P1 | 需用户确认权利；不可用时引导本地上传 |
| SCOPE-012 | 内置示例音频 | P1 | 让用户不上传也能体验结果页 |
| SCOPE-013 | 不可枚举的结果分享链接 | P1 | 结果随临时文件一起过期 |
| SCOPE-014 | 转录质量校准与回归 | P0 | 固定阈值配置、原始 MIDI 音符数基线与人工质量记录，防止参数变更悄悄降低召回率 |
| SCOPE-015 | 可读谱面质量流水线 | P0 | 在原始 MIDI 之外增加音符清洗、节拍/调性分析、和弦与多声部重建、质量标记 |
| SCOPE-016 | 混音分离实验适配器 | P1 | 为完整混音多轨转录提供隔离适配层；只有分轨和逐轨转录双门禁通过后按开关启用 |
| SCOPE-017 | 专用钢琴转录模型 A/B | P1（暂缓） | Basic Pitch 保持基线；候选先通过目标生产用途的代码、权重与数据许可审计，再投入同一真值集 A/B，只有质量与时延继续过门禁才可替换默认模型；当前无许可合格候选，不阻塞确定性后处理上线 |
| SCOPE-018 | 单乐器模式与乐器路由 | P1 | 用户明确选择目标乐器；保存规范化乐器 ID、谱表、移调和模型版本，不自动猜测后静默改路由 |
| SCOPE-019 | 单音家族转录 | P1 | 贝斯、小提琴、长笛、降 B 调小号、中音/次中音萨克斯和人声旋律；输出 MIDI 与单行五线谱 |
| SCOPE-020 | 吉他标准记谱转录 | P1 | 支持单乐器吉他录音的复音 MIDI 与标准五线谱；首批不生成 TAB 或 Guitar Pro |
| SCOPE-021 | 鼓转录与打击乐谱 | P1 | 输出 General MIDI Channel 10 打击乐事件、鼓时间线和 MusicXML 打击乐谱 |
| SCOPE-022 | 完整混音多轨转录 | P1 | 先分轨再逐轨转录，并与直接多乐器 AMT 做 A/B；仅返回通过质量门禁的已知轨道 |

### 2.2 不在本版本范围

| 编号 | 内容 | 原因 |
|---|---|---|
| OUT-001 | 面向用户提供母带级人声/鼓/贝斯/伴奏分轨下载 | 分离 stem 仅是转录中间产物，不承诺达到混音制作所需的无伪影质量 |
| OUT-002 | 完整乐谱编辑器 | 撤销、声部、跨小节标记等会吞掉 MVP 周期 |
| OUT-003 | 单音符精细编辑 | 首版只验证自动转录与导出价值 |
| OUT-004 | 账号、订阅、支付和 credits | 尚未验证付费意愿，不先建设商业外壳 |
| OUT-005 | 教师批注、作业和班级管理 | 不是首版主用户任务 |
| OUT-006 | 原生移动 App | 响应式 Web 足够验证 |
| OUT-007 | 自有模型训练和数据标注平台 | 首版优先验证产品工作流 |
| OUT-008 | Guitar Tabs、Guitar Pro、人声歌词识别与歌词逐字对齐 | 已确认首批多乐器输出只做 MIDI 与对应乐谱，这些属于独立产品能力 |
| OUT-009 | 永久保存用户音频和转录结果 | 匿名 MVP 不承担长期媒体库职责 |
| OUT-010 | 从完整混音可靠拆出任意指定管弦乐器 | 通用 Demucs 只稳定覆盖常见 stem；在专用分离或直接多乐器 AMT 通过固定集前不得承诺 |

## 3. 用户任务

| 编号 | 用户任务 | 用户类型 | 优先级 |
|---|---|---|---|
| TASK-001 | 用户提交需要转成音符数据的钢琴录音片段 | 独立音乐制作人 | P0 |
| TASK-002 | 用户判断自动转录结果是否可用 | 独立音乐制作人 | P0 |
| TASK-003 | 用户把转录结果带入后续制作或记谱工具 | 独立音乐制作人 | P0 |
| TASK-004 | 用户通过在线视频链接获得可转录音频 | 独立音乐制作人 | P1 |
| TASK-005 | 用户把临时结果分享给协作者 | 独立音乐制作人 | P1 |
| TASK-006 | 用户判断生成的谱面哪些部分可信、哪些部分需要修改 | 独立音乐制作人 | P0 |
| TASK-007 | 用户为单乐器录音选择正确的目标乐器和谱面类型 | 独立音乐制作人 | P1 |
| TASK-008 | 用户把不同乐器或人声旋律转换为统一可编辑的 MIDI 与对应乐谱 | 独立音乐制作人 | P1 |
| TASK-009 | 用户从完整混音中获得多个可独立检查和下载的转录轨道 | 独立音乐制作人 | P1 |

## 4. 用户流程

### FLOW-001：本地音频转录

**关联任务：** TASK-001、TASK-002、TASK-003  
**优先级：** P0  
**目标：** 从本地钢琴音频得到可验证和下载的 MIDI、MusicXML 与 PDF。

**入口：** 首页首屏上传区域。

**主路径：**

1. 用户选择 MP3、WAV 或 M4A 文件。
2. 系统校验格式、大小和音频时长，并显示波形。
3. 音频超过默认长度时，用户在波形上选择最长 90 秒的片段。
4. 用户勾选“我拥有处理该内容所需的权利”。
5. 用户提交转录任务。
6. 系统上传选定片段，创建匿名任务并显示阶段进度。
7. 后端完成音频规范化、钢琴转录、MIDI 后处理、MusicXML 和预览数据生成。
8. 结果页加载五线谱、钢琴卷帘和原音频播放器。
9. 用户播放对比、调整速度、设置循环或整体转调。
10. 用户下载 MIDI、MusicXML 或 PDF。

**分支路径：**

- 文件超过 90 秒：必须先截取，不上传整段。
- 用户刷新页面：通过 URL 中的高熵任务 ID 恢复任务进度或结果。
- 转录失败：保留任务错误码，允许在限流范围内重试。
- 结果已过期：展示过期状态并引导重新提交。

**完成状态：**

用户看到可播放的五线谱和钢琴卷帘，并至少能成功下载 MIDI。

### FLOW-002：YouTube 链接导入

**关联任务：** TASK-004  
**优先级：** P1  
**目标：** 在技术与平台规则允许时，把用户有权处理的公开视频片段送入同一转录流程。

**主路径：**

1. 用户粘贴 YouTube 视频 URL。
2. 系统校验域名、URL 结构和可访问性。
3. 用户确认拥有必要权利并选择最长 90 秒区间。
4. 后端获取允许处理的音频片段，随后进入 FLOW-001 第 6 步。

**边界情况：**

- 私密、地区限制、登录限制、直播、超时或平台拒绝：不得绕过限制，提示改用本地上传。
- 不支持的域名：直接拒绝，不尝试通用下载。

### FLOW-003：转录质量复盘

**关联任务：** TASK-002、TASK-006
**优先级：** P0
**目标：** 用户能区分原始模型候选、清洗后的音符和谱面排版假设，不把可解析文件误认为正确乐谱。

**主路径：**

1. 后端保留原始模型 MIDI 与原始音符事件，不覆盖原始证据。
2. 系统计算置信度、重复音符、短碎片、重叠声部、节拍/调性/拍号置信度。
3. 系统生成清洗后的时间线，并将同时发生的音符组织为和弦或独立声部。
4. 系统生成 MusicXML，并在结果元数据中显示“已识别结果”和“排版假设”的边界。
5. 结果页显示质量摘要、警告和可用产物；低质量时仍允许下载原始 MIDI 供 DAW 修订。

**失败分支：**

- 结构分析置信度不足：使用明确标注的默认拍号/调性，不伪装成原曲识别。
- 清洗会显著降低音符召回：保留原始结果，清洗版标记为实验结果并阻塞默认五线谱。
- MusicXML 解析或排版失败：不阻塞原始 MIDI 和钢琴卷帘，展示分层错误。

### FLOW-004：单乐器多类型转录

**关联任务：** TASK-007、TASK-008
**优先级：** P1
**目标：** 用户上传单乐器或已分轨音频，选择目标乐器后得到符合该乐器语义的 MIDI、时间线和乐谱。

**主路径：**

1. 用户选择“单乐器”输入模式和目标乐器；萨克斯必须进一步选择中音或次中音。
2. 系统显示该乐器的输出范围和限制，例如人声不含歌词、吉他不含 TAB。
3. 用户上传并截取音频，系统按乐器路由到已通过门禁的模型与后处理配置。
4. 系统保存响音高 MIDI 作为规范数据，并按目标乐器生成正确谱表、移调和 General MIDI program。
5. 结果页显示乐器名称、模型版本、置信度、MIDI、时间线、乐谱和可用下载。

**失败分支：**

- 用户选错乐器或输入明显包含多种乐器：不静默切换模型，提示改选乐器或使用完整混音模式。
- 目标乐器模型未上线或质量门禁未通过：禁用提交并显示“尚未支持”，不得回退成钢琴模型后伪装成功。
- 人声出现和声、贝斯出现复音等超出首批单音假设的输入：保留原始结果并显示低置信度或范围警告。

### FLOW-005：完整混音多轨转录

**关联任务：** TASK-009
**优先级：** P1
**目标：** 用户上传完整歌曲后获得若干可独立检查的已知乐器转录轨，而不是一个混在一起的伪精确 MIDI。

**主路径：**

1. 用户选择“完整混音”模式并提交音频片段。
2. 系统先生成受支持的 stem 候选和分离质量摘要，不覆盖原始音频。
3. 每个已知 stem 独立进入对应乐器模型，生成独立 `TranscriptionTrack` 和产物。
4. 结果页按轨道显示原音、转录演奏、置信度、失败状态和下载入口。
5. 系统只把通过乐器与质量门禁的轨道标为已识别；无法拆分的内容保留为 `other` 或 `unknown`。

**失败分支：**

- 分轨整体失败：回退到用户手动选择目标乐器的单乐器模式，不返回伪造多轨。
- 单个 stem 转录失败：其他成功轨道仍可查看和下载，失败轨道显示具体错误。
- 管弦乐器混在 `other`：只有直接多乐器 AMT 也通过该乐器固定集门禁时才生成候选，否则明确显示“未可靠识别”。

## 5. 功能需求

### REQ-001：音频上传与校验

**优先级：** P0  
**关联任务：** TASK-001  
**关联流程：** FLOW-001

**行为：** 用户拖放或选择文件后，浏览器先做基础校验并解析时长，再进入片段选择。

**规则：**

- MUST 支持 MP3、WAV、M4A。
- MUST 拒绝超过 25 MB、无法解码、时长为 0 或伪装 MIME 类型的文件。
- MUST 在上传前要求版权确认。
- SHOULD 在浏览器端提取波形，避免为无效文件创建任务。

**输入：**

| 字段 | 类型 | 必填 | 校验规则 |
|---|---|---:|---|
| audio_file | File | Yes | MP3/WAV/M4A；1 B–25 MB；必须可解码 |
| rights_confirmed | Boolean | Yes | 必须为 true |

**验收标准：**

- [ ] AC-001：Given 用户选择合法音频，when 浏览器完成解析，then 页面在 3 秒内显示文件名、时长和波形占位或波形。
- [ ] AC-002：Given 文件格式或大小非法，when 用户选择文件，then 不创建任务并展示具体修复方式。
- [ ] AC-003：Given 用户未确认权利，when 点击转录，then 提交按钮保持不可用并解释原因。

### REQ-002：波形截取

**优先级：** P0  
**关联任务：** TASK-001  
**关联流程：** FLOW-001

**规则：**

- MUST 默认选择前 30 秒；不足 30 秒则选择全长。
- MUST 将选择区间限制在 1–90 秒。
- MUST 允许拖动区间和输入开始、结束时间。
- SHOULD 支持播放选区。

**验收标准：**

- [ ] AC-004：Given 音频超过 90 秒，when 用户扩大选区，then 选区被限制为 90 秒并显示提示。
- [ ] AC-005：Given 合法选区，when 用户提交，then 后端只接收或只处理所选区间。

### REQ-003：异步转录任务

**优先级：** P0  
**关联任务：** TASK-001  
**关联流程：** FLOW-001

**行为：** 后端将转录拆为上传、预处理、转录、乐谱生成和完成五个可观察阶段。

**规则：**

- MUST 返回不可枚举的任务 ID。
- MUST 支持幂等查询，不因页面轮询重复执行任务。
- MUST 为每个阶段记录结构化错误码。
- MUST 失败时允许重试，但不得无限重试。
- SHOULD 在 10 秒无新进度后展示“仍在处理”，而不是伪造百分比。

**验收标准：**

- [ ] AC-006：Given 提交成功，when 页面进入任务页，then 用户能看到当前真实处理阶段。
- [ ] AC-007：Given 页面刷新，when 任务仍存在，then 页面恢复进度或结果，不重新上传。
- [ ] AC-008：Given 转录服务失败，when 任务结束，then 页面展示可操作的错误信息和重试入口。

### REQ-004：AI 钢琴转录与后处理

**优先级：** P0  
**关联任务：** TASK-001、TASK-002  
**关联流程：** FLOW-001

**行为：** 将规范化后的钢琴音频转换为带音高、起止时间和力度的 MIDI 事件，并生成适合基础乐谱显示的量化结果。

**规则：**

- MUST 只宣称支持以钢琴为主体的音频。
- MUST 保留原始模型输出 MIDI，并另生成乐谱量化版本。
- MUST 对无有效音符、模型异常和超时分别报错。
- MUST 使用经固定真实钢琴测试集校准的检测阈值；不得以未验证的高阈值换取表面的低误音率而系统性漏掉音符。
- MUST 在模型版本中记录检测阈值配置，并在每次模型或阈值变更后保存固定测试集的原始 MIDI 音符数与人工评审结果。
- SHOULD 推断基础速度；拍号与左右手分配无法可靠推断时，必须作为可解释的排版默认值，而非声称识别出的原曲结构。
- MUST NOT 将低置信度结果包装成“准确乐谱”。

**AI 能力规格：**

| AI 功能 | 能力类型 | 质量条 | 触发方式 | 不确定时 | 服务降级 |
|---|---|---|---|---|---|
| 钢琴 Audio-to-MIDI | 音频理解 / 序列转录 | 固定测试集 10 段中至少 7 段可直接使用或只需少量修改；快速复调基准须通过参数 A/B 排除系统性漏音；低于 6 段阻塞发布 | 用户主动提交后自动执行 | 标注“结果可能需要修正”，不生成虚假精度分数 | 排队、超时失败并允许重试；不得返回伪造结果 |
| MIDI-to-score 后处理 | 结构化转换 | 所有成功 MIDI 均能生成可解析 MusicXML；节拍、拍号和左右手质量进入人工回归集 | 转录成功后自动执行 | 使用 4/4、推断 BPM 和中央 C 分手等默认规则时明确标注为排版假设 | 保留 MIDI 下载；MusicXML/PDF 单独显示失败 |

**AI 护栏：**

- 不声称识别结果与原曲或人工谱面完全一致。
- 不以用户上传内容训练模型，除非未来另行获得明确同意。
- 不绕过视频平台访问、登录、地区或付费限制。
- 不在无有效音符时生成空白但标记成功的乐谱。

**验收标准：**

- [ ] AC-009：Given 固定 10 段测试集，when 执行完整转录，then 至少 7 段达到已定义的可用等级。
- [ ] AC-010：Given 模型未检测到有效音符，when 任务结束，then 返回 NO_NOTES_DETECTED，而不是成功空结果。
- [ ] AC-011：Given 转录成功，when 后处理完成，then MIDI 可被标准 MIDI 解析器打开，MusicXML 可被乐谱渲染器解析。
- [ ] AC-018：Given 快速复调的固定 Bach 基准，when 比较候选阈值与已确认的召回基线，then 候选不得降低原始 MIDI 音符数，且人工评审不得降级。
- [ ] AC-019：Given 乐谱采用默认 4/4 拍号或中央 C 分手，when 用户查看结果，then 系统不得将其表述为已识别出的原曲拍号或手部分配。

### REQ-005：结果工作台

**优先级：** P0  
**关联任务：** TASK-002  
**关联流程：** FLOW-001

**布局：** 桌面端为左侧任务摘要、中央主预览、底部播放控制、右侧导出操作；移动端折叠为单列，不要求适合复杂编辑。

**规则：**

- MUST 可切换五线谱和钢琴卷帘。
- MUST 将钢琴卷帘呈现为纵向下落的音符与底部钢琴键盘；音符抵达固定击键线时同步高亮对应琴键。
- MUST 支持播放、暂停、定位、0.5×–1.5× 变速、循环选区。
- MUST 支持整体 ±12 半音预览和导出。
- MUST 允许原始录音与转录演奏播放切换，避免同时播放造成混淆；钢琴转录演奏必须使用可随项目分发的真实钢琴采样，采样加载失败时才允许降级为基础合成音并明确提示。
- SHOULD 在播放器中同步当前音符位置。

**状态：**

- 加载：显示任务阶段和骨架屏。
- 空状态：未检测到音符，给出输入改进建议。
- 错误：按上传、转录、MusicXML、PDF 分层显示。
- 成功：默认显示五线谱，下载按钮按产物可用性启用。
- 无权限 / 过期：任务不存在或已删除时，不泄露是否曾存在，统一引导重新提交。

**验收标准：**

- [ ] AC-012：Given 结果成功，when 用户切换视图，then 五线谱和钢琴卷帘使用同一套音符时间数据。
- [ ] AC-013：Given 用户选择转调，when 播放或下载，then预览与导出结果使用相同半音偏移。
- [ ] AC-014：Given 375 px 宽屏幕，when 打开结果页，then 无页面级横向滚动且核心播放、切换、下载可用。
- [ ] AC-020：Given 钢琴卷帘播放，when 音符抵达击键线，then 使用与转录演奏相同的时间线高亮对应琴键，并按左右手区分颜色。
- [ ] AC-043：Given 用户播放钢琴转录演奏，when 本地钢琴采样加载成功，then 所有 MIDI 音符使用真实钢琴采样发声；加载失败时播放可降级但不得把基础合成音表述为钢琴音色。

### REQ-006：格式导出

**优先级：** P0  
**关联任务：** TASK-003  
**关联流程：** FLOW-001

**规则：**

- MUST 导出标准 MIDI 文件。
- MUST 导出可被 OpenSheetMusicDisplay 和至少一种外部记谱软件解析的 MusicXML。
- MUST 从当前乐谱渲染结果生成 PDF。
- MUST 对每种产物独立标记成功或失败，PDF 失败不得阻塞 MIDI 下载。

**验收标准：**

- [ ] AC-015：Given 结果成功，when 下载 MIDI，then 文件可被测试 DAW 或 MIDI 解析器打开且至少包含一个音轨和一个音符。
- [ ] AC-016：Given MusicXML 生成成功，when 加载到 OSMD，then 页面无解析错误并显示至少一个小节。
- [ ] AC-017：Given 乐谱已渲染，when 导出 PDF，then PDF 至少一页、可打开、包含标题和五线谱且不截断页面主体。

### REQ-007：匿名访问、限流与生命周期

**优先级：** P0  
**关联流程：** FLOW-001

**规则：**

- MUST 允许未登录用户使用。
- MUST 对 IP 和任务令牌组合限流；默认每 IP 每小时 3 次提交、同时最多 1 个运行任务。
- MUST 使用至少 128 bit 随机任务 ID，禁止递增 ID。
- MUST 在任务创建 24 小时后删除原音频、中间文件和导出产物。
- MUST 提供立即删除入口；删除后任务链接不可恢复。
- SHOULD 在首页明确展示保存期限。

### REQ-008：YouTube 链接导入

**优先级：** P1  
**关联任务：** TASK-004  
**关联流程：** FLOW-002

**规则：**

- MUST 只接受 youtube.com 和 youtu.be 的 HTTPS URL。
- MUST 要求用户确认权利。
- MUST NOT 绕过私密、登录、地区、付费或平台技术限制。
- MUST 在下载能力不可用时降级为本地上传，不阻塞 P0 发布。
- SHOULD 只获取用户选择的最长 90 秒音频，避免保存完整视频。

### REQ-009：音乐理解与可读谱面重建

**优先级：** P0
**关联任务：** TASK-002、TASK-006
**关联流程：** FLOW-001、FLOW-003

**行为：** 在 Basic Pitch 原始音符事件和最终 MusicXML 之间增加可重复、可解释的质量流水线，分别保存原始候选和清洗后的谱面数据。

**规则：**

- MUST 保留原始模型 MIDI、原始音符事件、清洗后音符事件和最终 MusicXML 的产物关系，不能用清洗结果覆盖原始证据。
- MUST 对低置信度音符、重复事件、极短碎片、异常延音和同音重复执行可配置的清洗策略；每次删除、裁剪或保留都记录原因和数量。缺少独立重触键证据时，不得把不同起音的相邻同音自动合并。
- MUST 使用 beat/downbeat 网格辅助量化；暂时无法可靠推断拍号或调性时，使用默认值并标记置信度与假设来源。
- MUST 将同一时刻的多音组织为和弦或合法独立声部；同一 voice 内不得出现非法时间重叠，不得把重叠音符全部塞进 voice 0。
- MUST 将左右手分配视为带置信度的后处理结果；不能只用中央 C 硬切后宣称识别了原曲分手。
- MUST 在谱面生成前运行结构校验：小节时值守恒、声部重叠、和弦数量、异常休止符、调号/拍号一致性和 MusicXML 解析。
- MUST 将 Basic Pitch 作为可回滚基线；专用钢琴转录模型与 Omnizart 等候选必须先证明代码、权重和训练数据许可允许目标生产用途，许可不兼容时直接标为 `research_only` 并停止 GPU、固定集 A/B 和人工盲评投入；许可通过后才进入同一固定集 A/B，不得因为单个示例听起来更像就替换生产默认值。
- MUST 在没有许可合格候选时继续使用 Basic Pitch，并允许音符清洗、结构分析和谱面重建按各自门禁继续交付；模型替换延期不得阻塞确定性后处理。
- SHOULD 首选 `librosa` 作为节拍、起音和调性分析的实验基线；`madmom`、Essentia 或专用和弦模型必须先通过同一数据集对比再决定是否进入生产依赖。
- MUST NOT 默认引入 Demucs 处理纯钢琴录音；只有混音检测和分离质量评测通过后，才能以 P1 feature flag 运行。
- MUST 在结果页区分“原始模型结果”“清洗后预览”和“排版假设”，不输出未经验证的准确率。

**AI 能力规格：**

| AI/分析功能 | 能力类型 | 质量条 | 触发方式 | 不确定时 | 服务降级 |
|---|---|---|---|---|---|
| 钢琴转录模型选择 | 自动音乐转录 / AMT | 候选先通过目标生产用途许可审计，再至少使固定 10 段可用数从基线 7/10 提升到 8/10，且快速复调召回不得下降超过 5%、时延过门禁 | 仅对许可通过的候选执行离线 A/B；质量通过后再由版本开关灰度 | 无许可合格候选或证据不足时继续使用 Basic Pitch | 候选加载或推理失败时回退 Basic Pitch，不影响原始输入 |
| 节拍、调性和拍号分析 | 音频特征分析 / MIR | 在结构回归集上记录 BPM 误差、拍号准确率和调性置信度；低置信度不得覆盖默认假设 | 每次转录成功后自动执行 | 显示“未可靠推断”，使用默认网格并保留标记 | 分析失败不阻塞原始 MIDI 和钢琴卷帘 |
| 音符清洗与合并 | 确定性后处理 | 清洗版不得使固定回归集原始音符召回下降超过 5%；人工可用段数不得下降 | 转录成功后自动执行 | 保留原始事件，清洗版降级为实验预览 | 清洗失败回退原始时间线，MusicXML 单独失败 |
| 和弦与多声部重建 | 符号化结构推断 | 同一 voice 无非法重叠；结构回归集严重排版错误为 0；和弦/声部结果带置信度 | 清洗后自动执行 | 使用独立声部或标记“复音未确定”，不强行合并 | 生成可解析的基础单声部 MusicXML，并明确警告 |

**验收标准：**

- [ ] AC-021：Given 含重复、短碎片和低置信度事件的原始 MIDI，when 执行清洗，then 输出删除/合并计数、原因和未改动的原始 MIDI。
- [ ] AC-022：Given 结构回归集，when 执行节拍/调性分析，then 保存 BPM、beat grid、拍号/调性候选及置信度；低置信度时显示默认假设而不是确定结论。
- [ ] AC-023：Given 同一时刻出现两到四个音符，when 生成 MusicXML，then 结果使用 Chord 或合法多声部，不在同一 voice 产生非法重叠。
- [ ] AC-024：Given 左右手交叉、低音持续和中央 C 附近的音符，when 计算分手，then 输出分手置信度；无法判断时允许 `unknown`，不得以中央 C 规则伪装识别。
- [ ] AC-025：Given 至少 16 段的结构回归集，when OSMD 和 music21 解析 MusicXML，then 小节时值守恒、无严重重叠/异常休止符，且至少 80%（首版至少 13/16）片段被人工评为“谱面可读”。
- [ ] AC-026：Given 清洗或结构重建失败，when 用户查看结果，then 原始 MIDI 和钢琴卷帘仍可用，MusicXML/PDF 单独显示失败原因。
- [ ] AC-027：Given 同一音频、模型版本和后处理配置，when 重复执行两次，then 原始事件摘要、清洗计数和 MusicXML 结构结果一致。
- [ ] AC-028：Given Basic Pitch 与至少一个已证明代码、权重和训练数据允许目标生产用途的专用钢琴候选，when 在固定 10 段转录集和至少 16 段结构集执行隔离 A/B，then 报告音符指标、人工可用率、P95 时延和峰值内存；只有达到至少 8/10 可用且关键样本无降级时才允许灰度替换基线。许可不兼容的候选必须在 A/B 前标为 `research_only` 并停止后续评测投入。

### REQ-010：单乐器类型、模型路由与乐谱语义

**优先级：** P1
**关联任务：** TASK-007、TASK-008
**关联流程：** FLOW-004

**行为：** 用户明确选择单乐器类型，系统按稳定的乐器配置和模型路由生成对应 MIDI、时间线、五线谱与质量报告。

**乐器输出合同：**

| 乐器 ID | 首批输入假设 | MIDI 与乐谱输出 |
|---|---|---|
| `piano` | 独奏，可复音 | 现有双手 MIDI、钢琴大谱表和钢琴卷帘 |
| `bass` | 单音优先的贝斯 stem | 响音高 MIDI、低音谱表；复音输入显示范围警告 |
| `violin` | 单音小提琴 stem | 响音高 MIDI、单行高音谱表 |
| `flute` | 单音长笛 stem | 响音高 MIDI、单行高音谱表 |
| `trumpet_bb` | 单音降 B 调小号 stem | MIDI 保存响音高，MusicXML 写入降 B 移调信息并按记谱音高显示 |
| `sax_alto_eb` | 单音中音萨克斯 stem | MIDI 保存响音高，MusicXML 写入降 E 移调信息并按记谱音高显示 |
| `sax_tenor_bb` | 单音次中音萨克斯 stem | MIDI 保存响音高，MusicXML 写入降 B 与八度移调信息并按记谱音高显示 |
| `guitar` | 独奏，可复音 | MIDI 保存响音高，标准高音谱表按吉他八度移调记谱；不生成 TAB/Guitar Pro |
| `drums` | 鼓 stem | General MIDI 人类编号 Channel 10（代码零基索引 9）、标准打击乐映射、无音高鼓时间线和打击乐谱 |
| `voice_melody` | 单主唱旋律 | 响音高 MIDI、单行高音谱表；不输出歌词、音素或和声分轨 |

**规则：**

- MUST 要求用户在单乐器模式明确选择乐器；可以给检测建议，但未经确认不得静默改选。
- MUST 通过 `InstrumentProfile` 配置谱表、响音域、移调、复音能力、General MIDI program、模型和后处理策略，禁止散落乐器条件分支。
- MUST 将响音高作为 MIDI 和内部时间线的规范值；移调乐器只在 MusicXML/谱面层应用 written-pitch 规则，防止播放和导出相差固定音程。
- MUST 为每个乐器独立保存模型版本、配置版本、质量摘要和固定回归集；一个乐器通过不得自动放行另一个乐器。
- MUST 为非钢琴结果移除左右手语义；鼓不得复用有音高键盘，使用固定鼓件 lane；人声不得显示歌词功能占位。
- MUST 按已确认顺序发布：钢琴质量完成后，先单音家族，再吉他，再鼓。
- SHOULD 以 Basic Pitch 作为有音高单乐器的跨乐器基线；单音家族对比 pYIN/CREPE 类音高跟踪，鼓与人声对比 Omnizart 等任务模型，均只在固定集过门禁后上线。

**AI 能力规格：**

| AI/分析功能 | 能力类型 | 质量条 | 触发方式 | 不确定时 | 服务降级 |
|---|---|---|---|---|---|
| 单音家族转录 | 单音音高跟踪 + note segmentation | 每个乐器至少 10 段固定集、note F1 不低于 0.70（50 ms onset 容差）且至少 7/10 人工可用 | 用户选择对应乐器后自动执行 | 显示低置信度和复音/噪声范围警告 | 候选失败回退该乐器已批准基线；没有基线则任务失败，不回退钢琴 |
| 吉他转录 | 复音 AMT | 至少 10 段固定集、note F1 不低于 0.70 且至少 7/10 人工可用，和弦关键样本不得系统性漏音 | 用户选择吉他后自动执行 | 标记复音、延音和奏法不确定性 | 保留原始候选 MIDI，乐谱失败不阻塞 MIDI |
| 鼓转录 | 打击事件分类 | 至少 10 段固定集、50 ms onset 容差下 macro F1 不低于 0.70 且至少 7/10 人工可用 | 用户选择鼓后自动执行 | 未知鼓件映射为 `unknown_percussion`，不得伪装成军鼓或底鼓 | 模型失败则任务失败并允许重试，不生成有音高 MIDI |
| 人声旋律转录 | 单主旋律音高跟踪 | 至少 10 段固定集、voicing recall 与 note F1 均不低于 0.70，至少 7/10 人工可用 | 用户选择人声旋律后自动执行 | 和声或说唱段标为范围外/低置信度 | 不输出歌词；模型失败保留原音播放和错误信息 |

**验收标准：**

- [ ] AC-029：Given 单乐器模式，when 用户未选择乐器，then 提交保持不可用；选择乐器后任务保存规范化 `instrument_id` 和 profile 版本。
- [ ] AC-030：Given 任一已上线乐器，when 创建任务，then 模型路由只使用该乐器已批准版本；模型不可用时返回明确错误，不静默调用钢琴模型。
- [ ] AC-031：Given 降 B 小号、中音萨克斯或次中音萨克斯样本，when 导出 MIDI 与 MusicXML，then MIDI 播放为响音高，MusicXML 带正确移调元数据且谱面显示记谱音高。
- [ ] AC-032：Given 贝斯、小提琴、长笛、小号、萨克斯或人声旋律结果，when 打开结果页，then 显示单乐器时间线和对应单行谱表，不出现左右手标签。
- [ ] AC-033：Given 复音吉他样本，when 转录成功，then 输出可解析 MIDI 与标准五线谱，并明确不提供 TAB/Guitar Pro。
- [ ] AC-034：Given 鼓 stem，when 转录成功，then MIDI 使用人类编号 Channel 10（代码零基索引 9）与标准鼓件映射，MusicXML 使用打击乐谱，结果页使用鼓件 lane 而非钢琴键盘。
- [ ] AC-035：Given 人声输入，when 转录成功，then 只显示主旋律 MIDI/乐谱和范围说明，不生成或暗示存在歌词识别结果。
- [ ] AC-036：Given 任一新增乐器，when 发布门禁运行，then 该乐器拥有至少 10 段固定真值集、机器指标、人工评级、P95 时延、峰值内存和许可报告，并同时通过本需求的数值门槛。
- [ ] AC-037：Given 多乐器功能开启，when 重跑钢琴固定集和现有 E2E，then 钢琴质量、默认路由、双手谱面、钢琴卷帘和旧任务响应不得降级。

### REQ-011：完整混音分轨与多轨转录

**优先级：** P1
**关联任务：** TASK-009
**关联流程：** FLOW-005

**行为：** 对完整混音生成多个带来源和质量信息的候选轨道，并仅对可验证的已知乐器轨执行转录。

**规则：**

- MUST 将分轨、乐器识别和音符转录视为三个独立概率阶段，分别保存模型版本、置信度、耗时和错误；不得用最终音符置信度冒充分轨置信度。
- MUST 保留原始混音和内部 stem 的父子关系；stem 遵循与上传音频相同的 24 小时删除和立即删除规则，不默认提供母带级 stem 下载。
- MUST 支持轨道级部分成功：一个轨道失败不得阻塞其他成功轨道，任务状态必须表达 `partial_success`。
- MUST 将 Demucs 限制写入产品边界：默认四轨仅有人声、鼓、贝斯、other；实验六轨增加吉他和钢琴但钢琴存在明显串音风险；不得从 `other` 直接宣称识别出小提琴、长笛、小号或萨克斯。
- MUST 在同一混音固定集比较“分轨后单乐器模型”和 MT3 类直接多乐器 AMT；直接模型只有在目标乐器 program、音符质量、许可和运行预算全部过门禁后才能产生已识别轨道。
- MUST NOT 因完整混音功能上线而改变单乐器模式的默认模型、时延预算或质量门禁。

**AI 能力规格：**

| AI/分析功能 | 能力类型 | 质量条 | 触发方式 | 不确定时 | 服务降级 |
|---|---|---|---|---|---|
| 混音分轨 | 音乐源分离 | 至少 10 段混音固定集；每个声明支持的 stem 至少 7/10 被人工评为可用于后续转录，且分轨后转录不低于直接输入 | 用户选择完整混音模式后执行 | 输出 `other`/`unknown` 和串音警告，不强行命名 | 整体失败引导单乐器模式；单 stem 失败不阻塞其他轨 |
| 直接多乐器转录 | 多任务多轨 AMT | 每个输出 program 单独达到对应乐器门槛；不得只看全局平均 F1 | 仅完整混音实验开关开启时执行 A/B | 乐器 program 不确定时输出实验候选，不进入默认下载 | 模型不可用时保留分轨后单乐器路径 |

**验收标准：**

- [ ] AC-038：Given 完整混音，when 分轨与转录完成，then 每个轨道分别返回 `instrument_id`、来源方法、分轨置信度、转录置信度、模型版本、产物和状态。
- [ ] AC-039：Given Demucs 四轨或六轨结果，when 某管弦乐器仍位于 `other`，then API 和 UI 不得把它标成具体管弦乐器，除非直接多乐器模型通过该乐器门禁。
- [ ] AC-040：Given 一个 stem 转录失败，when 用户查看结果，then 任务显示部分成功，其他成功轨道仍可播放和下载，失败轨道可单独重试。
- [ ] AC-041：Given 用户删除完整混音任务或任务过期，when 清理完成，then 原始混音、全部 stem、轨道产物和质量报告均被删除。
- [ ] AC-042：Given 完整混音固定集，when 比较分轨后转录与直接多乐器 AMT，then 只有同时通过逐乐器质量、许可、P95 时延和峰值内存门槛的路径才能进入默认生产路由。

## 6. 数据模型

### 6.1 核心实体

| 实体 | 描述 | 关键字段 |
|---|---|---|
| TranscriptionJob | 一次匿名转录任务 | id、status、stage、source_type、source_mode、requested_instrument、start_sec、end_sec、created_at、expires_at、error_code |
| SourceAsset | 临时输入音频 | job_id、storage_key、mime_type、size_bytes、duration_sec、sha256 |
| StemAsset | 完整混音产生的内部临时 stem | job_id、stem_type、source_model、source_confidence、storage_key、sha256、expires_at |
| TranscriptionTrack | 一个乐器或人声轨的转录结果 | job_id、stem_asset_id、instrument_id、instrument_profile_version、route_source、status、separation_confidence、transcription_confidence、model_version、postprocess_version |
| TranscriptionResult | 任务级摘要与旧钢琴响应兼容层 | job_id、tempo、time_signature、key、track_count、quality_flags、confidence_summary |
| Artifact | 可下载产物 | job_id、track_id、type、storage_key、size_bytes、created_at |
| AnalyticsEvent | 不含原音频的产品事件 | event_name、job_id_hash、timestamp、properties |

### 6.2 实体关系

| 关系 | 描述 |
|---|---|
| TranscriptionJob has one SourceAsset | 每个任务对应一个临时输入 |
| TranscriptionJob has zero or one TranscriptionResult | 失败任务可以没有结果；旧钢琴任务继续返回兼容摘要 |
| TranscriptionJob has many StemAssets | 单乐器任务为零；完整混音任务可有多个临时 stem |
| TranscriptionJob has many TranscriptionTracks | 单乐器任务通常一个，完整混音任务可部分成功多个 |
| TranscriptionTrack has many Artifacts | 每轨 MIDI、时间线、MusicXML、PDF 和诊断产物独立记录 |
| TranscriptionTrack has quality summary | 原始/清洗音符数、分轨置信度、转录置信度、结构错误和版本可追踪 |

### 6.3 数据规则

- 任务由匿名用户创建，通过高熵任务 ID 访问。
- 客户端不得获得对象存储永久公开地址，只使用短期签名 URL 或受控下载接口。
- 用户可立即删除任务及所有关联文件。
- 后台清理任务必须在 expires_at 后删除所有对象和数据库元数据。
- 分析事件不得包含原音频、YouTube 完整 URL 或可还原用户身份的信息。
- 原始事件和清洗事件必须可按同一任务、模型版本和后处理版本追溯；清洗策略变更不得静默覆盖旧产物。
- 乐器 ID 只允许来自版本化白名单；接口不得接受任意模型名、checkpoint 路径或 shell 参数。
- 删除或过期任务时必须级联删除 SourceAsset、StemAsset、TranscriptionTrack、Artifact 和质量报告，不留下中间 stem。

## 7. 外部依赖与建议技术路线

| 编号 | 依赖 | 用途 | 是否必需 | 备注 |
|---|---|---|---:|---|
| DEP-001 | Next.js 16.x / React | Web 前端和公开页面 | Yes | 当前查询版本 Next.js 16.2.12 |
| DEP-002 | WaveSurfer.js 7.x | 波形显示、播放和截取 | Yes | 当前查询版本 7.12.11 |
| DEP-003 | OpenSheetMusicDisplay 2.x | MusicXML 五线谱渲染 | Yes | 当前查询版本 2.1.0 |
| DEP-004 | Tone.js 15.x + @tonejs/midi 2.x | MIDI 播放和钢琴卷帘数据 | Yes | 当前查询版本分别为 15.1.22、2.0.28 |
| DEP-005 | FastAPI | 转录 API 与任务状态 | Yes | Python 服务，与 Web 前端分离部署 |
| DEP-006 | Spotify Basic Pitch 0.4.x | 开源 Audio-to-MIDI 基线模型 | Yes | 首版模型；上线前验证运行时兼容和许可证 |
| DEP-007 | music21 10.x | MIDI 量化、分手和 MusicXML 生成 | Yes | 当前查询版本 10.5.0 |
| DEP-008 | FFmpeg | 解码、裁剪、单声道化和重采样 | Yes | 部署镜像固定版本 |
| DEP-009 | PostgreSQL / 托管数据库 | 任务元数据 | Yes | 可用 Neon、Supabase 或同类服务 |
| DEP-010 | S3 兼容对象存储 | 临时音频和产物 | Yes | 必须支持生命周期或定时清理 |
| DEP-011 | Redis / 托管队列 | 异步任务、并发与重试 | Yes | 可用 Redis Queue、BullMQ 或云任务队列 |
| DEP-012 | Vercel | Next.js Web 部署 | Yes | 只部署 `web/`，通过 `NEXT_PUBLIC_API_URL` 连接容器 API |
| DEP-013 | Railway 或等价容器平台 | FastAPI、FFmpeg、模型 Worker 和清理任务 | Yes | 推荐 Railway；必须支持常驻进程、Cron 和 Dockerfile |
| DEP-014 | yt-dlp | YouTube P1 输入适配 | No | 上线前必须复核平台条款和地区合规 |
| DEP-015 | librosa 0.11.0 | BPM、beat grid、起音和调性分析实验基线 | No | 已由 Basic Pitch 间接锁定；先提升为显式依赖并离线评测，通过结构回归后再进入 Worker 默认路径 |
| DEP-016 | madmom / Essentia / 专用和弦模型 | 节拍、重拍、和弦标签和结构分析候选 | No | 不同时引入；madmom 模型数据和 Essentia AGPL 许可必须先审计，未批准不得进入生产镜像 |
| DEP-017 | Demucs 或经审计的维护分支 | 常见 stem 分离实验 | No | Meta 上游仓库已归档；四轨仅 vocals/drums/bass/other，实验六轨增加 guitar/piano 且 piano 串音风险高 |
| DEP-018 | piano_transcription_inference | 专用钢琴候选 | No | 仅隔离 A/B；以旧 Python/PyTorch 为基线且代码与权重许可需审计，不直接装入主 Worker |
| DEP-019 | librosa pYIN / CREPE | 单音家族音高跟踪候选 | No | pYIN 复用现有 librosa；CREPE 为 MIT 但版本较旧，必须与 Basic Pitch 按乐器 A/B |
| DEP-020 | Omnizart | 有音高乐器、人声旋律和鼓任务候选 | No | 代码 MIT；checkpoint 许可、Python 3.11/Linux 与 ARM 兼容需审计，候选依赖隔离 |
| DEP-021 | MT3 | 直接多乐器多轨 AMT 候选 | No | 代码 Apache-2.0 且非官方支持产品；T5X、checkpoint、GPU 成本和乐器 program 质量通过 A/B 前不进生产 |
| DEP-022 | Aria-AMT (`EleutherAI/aria-amt`) | 专用钢琴转录研究参考 | No | 代码 Apache-2.0，要求 Python 3.11、PyTorch >=2.3 与 torchaudio <=2.5；固定权重为 CC-BY-NC-SA-4.0，不允许目标生产用途，标为 `research_only`，保留许可与 provenance 审计，不分配 CUDA、不进入固定集 A/B 或主 Worker |

## 8. 非功能需求

| 类别 | 要求 | 优先级 |
|---|---|---|
| 性能 | 首页静态内容 LCP 目标不超过 2.5 秒；任务查询接口 P95 不超过 500 ms；转录时延按 1.6 节目标 | P0 |
| 安全 | 文件类型双重校验、随机文件名、无 shell 拼接、对象私有、下载签名、CSRF/CORS 限制、IP 限流 | P0 |
| 隐私 | 默认不训练；上传和产物 24 小时删除；支持立即删除；隐私说明展示在提交前 | P0 |
| 兼容性 | Chrome、Edge、Safari、Firefox 最近两个主要版本；桌面优先，375 px 起可完成核心流程 | P0 |
| 可靠性 | 任务幂等；Worker 崩溃可重试最多 2 次；单个产物失败不拖垮其他产物；提供结构化错误码 | P0 |
| 可访问性 | 键盘可完成上传、提交、播放与下载；控件有标签；颜色对比达到 WCAG 2.1 AA 基线 | P1 |
| 可观测性 | 记录 request_id、job_id、阶段耗时、模型版本、错误码；不得记录原始音频内容 | P0 |
| 成本 | 每 IP 每小时 3 次、同时 1 个任务；配置全局并发上限和每日任务熔断值 | P0 |
| 谱面质量 | 清洗后 MusicXML 同一 voice 不得非法重叠；结构校验失败不得标记完整成功；原始/清洗音符数量和原因可追踪 | P0 |
| 可重复性 | 同一音频、模型版本、分析依赖和后处理配置产生一致的质量摘要与结构化事件 | P0 |
| 多乐器隔离 | 单个乐器模型或单个 stem 失败不影响其他轨道；模型权重和重依赖按 Worker 类型隔离 | P1 |
| 多乐器性能 | 单乐器沿用 30 秒 P95 180 秒、90 秒 P95 360 秒；完整混音暂定 30 秒 P95 360 秒、90 秒 P95 720 秒，超出即阻塞默认上线 | P1 |
| 向后兼容 | 旧钢琴任务、`midi`/`timeline` 产物、下载 URL 和钢琴结果页必须保持可读；新增轨道字段只做向后兼容扩展 | P0 |

## 9. 验证数据与测试方法

- 建立 10 段固定独奏钢琴音频回归集，每段 30 秒，覆盖慢速旋律、快速音阶、和弦、踏板、弱音、强音、轻微背景噪声和不同录音设备。
- 每段结果由至少一名具备 MIDI/DAW 使用经验的人按三级评估：可直接使用、少量修改可用、需要重做。
- “少量修改”暂定义为：30 秒片段内不超过 10 个明显错音或漏音，且无需整体重建节拍网格；该阈值属于待真实用户校准的假设。
- 每次模型、检测阈值、量化或后处理变更必须重跑固定数据集，保存原始 MIDI 音符数、模型配置、结果摘要和人工评审。
- 新增至少 16 段的结构回归集，覆盖：单音旋律、密集和弦、快速复调、延音踏板、左右手交叉、3/4 或 6/8 节奏、调号变化和混合背景噪声；每类至少 2 段，并保存人工校对的音符、节拍、拍号、调性和谱面评级真值。
- 质量报告至少记录：音符 precision/recall、起音误差、音高误差、原始/清洗音符数、删除/合并数、非法声部重叠数、小节时值错误数、拍号/调性置信度和人工等级。
- 模型 A/B 必须同时记录 Basic Pitch 与候选模型的音符指标、10 段人工可用数、P95 推理时延、峰值内存、运行环境和模型权重许可；没有完整报告不得更换默认模型。
- 每个新增乐器建立至少 10 段、每段 30 秒的独立真值集；有音高乐器保存音符/起止/移调真值，鼓保存标准鼓件与 onset 真值，人声只标主旋律和 voicing。
- 建立至少 10 段完整混音固定集，包含可获得独立 stem 真值的歌曲；分别评估分轨 SDR/串音、逐轨音符指标、乐器标签、人工可用率和资源成本。
- Demucs 或直接多乐器 AMT 只有在逐乐器质量不低于相应单乐器基线、且 P95 成本与时延在预算内时，才可进入完整混音线上实验。

## 10. 完成定义

- [ ] 所有 P0 requirements 已实现。
- [ ] 所有 P0 acceptance criteria 已通过。
- [ ] FLOW-001 可以在生产 URL 上端到端完成。
- [ ] 10 段固定测试集至少 7 段达到可用等级。
- [ ] 主要加载、空、错误、成功、过期状态已处理。
- [ ] MIDI、MusicXML、PDF 均经过真实文件解析或渲染验证。
- [ ] 上传内容能在 24 小时后自动删除，并可由用户立即删除。
- [ ] 生产环境完成基础限流、错误日志和隐私说明。
- [ ] P1 的 YouTube 功能失败或未上线时，不影响本地上传主流程。
- [ ] REQ-009 的原始/清洗产物、结构质量摘要和 MusicXML 质量门禁已通过；未通过时不得宣称生成了可读完整谱面。
- [ ] 专用钢琴候选许可审计已完成；存在许可合格候选时才执行 AC-028 A/B，未出现合格候选则继续使用 Basic Pitch，且不阻塞其余 P0 后处理上线。
- [ ] 每个对外显示“已支持”的新增乐器都单独通过固定集、文件解析、人工评级、许可和运行预算门禁；未通过的乐器在 UI 中不可提交。
- [ ] 单乐器模式不会把选错乐器、模型缺失或范围外输入伪装成成功结果。
- [ ] 完整混音模式通过轨道级部分成功、未知乐器边界、级联删除和旧钢琴回归测试后才可开启。

## 11. 假设与待确认问题

### 11.1 假设

| 编号 | 假设 | 假设依据 | 错误风险 |
|---|---|---|---|
| ASM-001 | 独立音乐制作人愿意使用钢琴 Audio-to-MIDI 工具 | 竞品和行业调研；无直接访谈 | 产品可能解决的是低频或低价值问题 |
| ASM-002 | 90 秒足够验证制作人是否认可结果质量 | 用户接受窄 MVP；竞品免费体验通常限制时长 | 真实任务可能需要整曲，测试结论失真 |
| ASM-003 | 经阈值校准后的 Basic Pitch 足以作为首版模型基线 | 成熟开源方案可快速落地；当前 A/B 已确认默认阈值能显著减少快速复调漏音 | 复音钢琴与踏板场景仍可能达不到 7/10 门槛 |
| ASM-004 | 4/4、推断 BPM 和基础左右手规则能产生可读 MusicXML | MVP 不做专业乐谱编辑 | 该规则不是原曲结构识别，PDF 可读性和记谱准确度可能成为主失败点 |
| ASM-005 | 匿名公开体验比先做账号系统更适合验证 | 用户选择公开网站，且尚无付费证据 | 滥用和推理成本可能高于预期 |
| ASM-006 | 单音家族可共享统一音高跟踪与 note segmentation 管线 | 这些乐器和主唱旋律首批按单主音处理 | 贝斯复音、和声、颤音和气声会导致统一管线失效 |
| ASM-007 | 用户接受先选乐器而不是完全自动识别 | 明确选择可避免错误模型路由并建立逐乐器质量证据 | 用户可能期待上传即自动判断，增加一步会降低完成率 |
| ASM-008 | 中音/次中音萨克斯和降 B 小号覆盖首批常见移调需求 | 可先验证 written pitch 与 concert pitch 架构 | 其他萨克斯和小号调性用户会认为范围不足 |
| ASM-009 | 完整混音的价值来自可编辑转录轨，不是母带级 stem 下载 | 用户目标仍是扒谱与 MIDI 编辑 | 若用户实际要分轨音频，本产品路线会偏离需求 |

### 11.2 待确认问题

| 编号 | 问题 | 是否阻塞 | 备注 |
|---|---|---:|---|
| Q-001 | 目标用户真实工作流、现用工具和愿付代价是什么 | No | 产品上线后优先访谈 5 名制作人 |
| Q-002 | 生产部署使用哪家数据库、对象存储和 Worker 平台 | No（已确认） | Vercel Web + Railway 容器/PostgreSQL/Redis + 私有 S3 兼容存储；实际资源由用户创建 |
| Q-003 | 用户可接受的单次转录成本和每日总预算 | No | 先通过配置限流和全局熔断控制 |
| Q-004 | YouTube 链接处理在目标运营地区是否合规 | Yes for P1 | 不阻塞 P0，本功能默认通过开关关闭 |
| Q-005 | “少量修改可用”的量化阈值是否符合制作人判断 | No | 上线后用真实用户校准 |
| Q-006 | 混音输入是否纳入产品承诺，还是继续只支持钢琴主体音频 | No（已确认） | 用户确认两者都支持：先单乐器，再完整混音；混音仍受逐轨质量门禁约束 |
| Q-007 | 拍号/调性自动推断的最低置信度阈值 | Yes for REQ-009 | 先用结构回归集校准，低于阈值统一显示默认假设 |
| Q-008 | 用户是否需要下载原始 MIDI 与清洗 MIDI 两种版本 | No | 默认下载清洗版，同时保留原始版作为高级诊断入口 |
| Q-009 | 首批是否需要 Guitar TAB/Guitar Pro 与人声歌词 | No（已确认） | 首批只输出 MIDI 和对应乐谱；吉他/贝斯 TAB 与歌词识别明确后置 |
| Q-010 | 多乐器发布顺序 | No（已确认） | 钢琴质量 → 单音家族 → 吉他 → 鼓 → 完整混音 |

## 12. 需求来源与优先级说明

- 竞品调研来源：用户提供的 songscription-competitive-research.md，调研日期 2026-07-28。
- 用户明确确认：主用户为制作人；项目定位为假设型 MVP；核心质量门槛为 10 段中至少 7 段可用；需要五线谱、MusicXML、PDF；YouTube 链接为 P1；音频最长 90 秒并支持波形截取；首版直接面向公网部署。
- 用户于 2026-07-31 将部署交付改为：使用 Vercel 作为网站入口，只需要提供部署方法和就绪配置，不代为创建或发布云资源。
- 用户于 2026-08-05 确认未来支持吉他、贝斯、小提琴、长笛、降 B 小号、中音/次中音萨克斯、鼓和人声旋律；先单乐器/已分轨输入，后完整混音；首批不做 TAB、Guitar Pro 或歌词识别。
- 当前不存在用户访谈证据。所有市场价值结论必须视为假设，不得在产品文案中伪造用户数量、准确率或商业效果。
