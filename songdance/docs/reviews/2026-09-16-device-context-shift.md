# 10-device 上下文边界对照

保持原规范化PCM16音频逐样本不变，在前面加4秒静音、后面加1秒静音，使用同一Transkun权重配置重新推理，输出减4秒回到原时间轴。不按参考MIDI选择、拼接音符，不改教师确认的右手谱面。

原输出87音，90个源事件中漏0/15/16秒C2。新输出的原区间内86音与参考全匹配，漏0秒C2、0秒C4、3秒C2、20秒C2。这里必须区分：0秒C4其实被模型检出，但映射为-0.003646秒，严格边界映射器把它排除，并保存在rejected_outside_original_onsets中。因此新增的C4漏记是本实验裁切规则造成，不能归咎于模型。

原15/16秒C2被重新检出，3/20秒C2消失，0秒C2仍未检出。这说明检测结果具有上下文敏感性，不能声称统一加静音已经修好漏音，也不能据参考位置选择性拼接两次结果。

新增纯衰减无重击对照测试：基频后窗小于前窗、不确认独立起音；时间映射测试显式保留裁切/排除日志。共7项局部测试通过。音频测量和上下文结果均为研究证据，未补入任何曲谱音符。

产物：api/tests/fixtures/audio/candidates/piano-model-comparison-expanded-v1/10-device/context-shift-4s，含原样本保持证明、真实推理receipt、aligned.mid和evaluation.json。准备脚本prepare_device_context_shift.py、模型运行器run_cpu.py、评价脚本evaluate_device_context_shift.py均在api内；报告绑定输入、模型输出及评价器指纹。

独立fresh审查因子任务数量上限无法启动，本轮尚未完成该验收环节。下一步须完善首音时间容差，并检验多次推理一致性与误补反例，不能宣布低音修复完成。

## 边界映射v2

将[-10ms,0)且实际跨入原片段的模型事件钳到0并记录before/after；完全位于静音区或更早起音仍拒绝。这是明确的评价映射容差，不表示检测到了真实0秒起音。新evaluation-v2.json/aligned-v2.mid保留旧报告。

首音C4恢复计数后，两版均87匹配/0额外/3漏音（50/100ms一致）。低音25个匹配交集；原版独有3/20秒C2、移位版独有15/16秒C2，开头C2两版都未检出。差异按两模型输出计算，不用参考谱选择合并。不存在一版整体更好的证据，不自动拼接。

新增近零起音修复与静音区事件不得虚构至零秒两个测试，相关共9 passed；Ruff、编译通过。尚未完成fresh独立复审，仍不生产接入。已确认的右手谱面不变。
