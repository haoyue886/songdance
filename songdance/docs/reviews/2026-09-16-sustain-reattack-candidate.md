# 06三音新源：同音重击分割实验

输入为06-teacher-triad-models-v1中的两模型原始时间映射事件及同一规范化音频。候选只能来自Basic Pitch，在唯一Transkun同音长事件内部，距前起音超过300ms、距尾超过100ms。参考MIDI不参与候选或阈值判断，只在输出后计算指标。

测量候选前后200ms Hann窗基频振幅，后窗>=.001且前后比>=4；若同期80ms内另一个较低模型音可在35cent内以2–6阶泛音解释目标音则弃权。这是研究阈值，不是已证明物理独立击键的检测器；渐强、拍频、复杂多声部及更多真实录音尚未覆盖。

本次5个候选均通过实验条件，前后基频幅度比约62–79，分割四个C4（2/8/20/26秒附近）及一个G3（18秒附近）的长事件，保留父事件起音/音高/总覆盖范围。不补4/22秒两个完全漏掉的G3。

输出43匹配/0额外/2漏音（50/100ms一致），相比原38/0/7恢复5个起音；不是45音完整通过。原始MIDI未修改，新候选在api/tests/fixtures/audio/candidates/06-teacher-triad-models-v1/reattack-v1/candidate.mid，audit.json记录每次建议、父事件、测量、竞争音和前后指纹。仍production_eligible=false。

7项新增测试包含真实重击正例、持续衰减/噪声/另一音起音/低八度泛音反例、无第二模型候选不补音以及本次完整产物重算；相关共11 passed，Ruff/编译通过。fresh独立复审尚未完成，不生成声称通过的曲谱，不接生产。
