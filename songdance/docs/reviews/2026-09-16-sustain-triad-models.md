# 06三音新源的双模型转录结果

使用独立06-teacher-triad-source-v1：15组和弦、45个参考事件，513/614/572循环。两模型接收完全相同的规范化WAV（前后各1秒静音），均保留原输出，只统一映射回原时间，不用参考补音或删音。此源是新的合同合成样本，不冒充已发送旧四音录音。

50/100ms结果一致：Basic Pitch匹配45/额外33/漏0，F1 .7317；Transkun匹配38/额外0/漏7，F1 .9157。两者都未同时达到45个正确且无额外音，06仍不放行，不生成宣称已修复的曲谱。

Transkun漏音位置：C4在2/8/20/26秒，G3在4/18/22秒。进一步核对模型原始时值：四个C4和18秒G3均落在此前同音模型事件内部，说明这五处表现为把重复击键表示成跨起音长音；4/22秒G3没有此前同音覆盖。这个分类只用于事后诊断，不用参考起音强行分割模型事件。下一步若修复，应分别验证同音重击与完全漏检，不能只把长音截短就声称恢复了漏掉的起音。

原始模型MIDI、Basic原始事件、输入WAV、源与参考哈希、模型receipt、失败状态以及全部匹配保存在api/tests/fixtures/audio/candidates/06-teacher-triad-models-v1。compare_sustain_triad_models.py拒绝覆盖输出并核验三音源receipt。7项相关测试通过（包含完整重算、同输入原样本验证、失败记录/禁止覆盖、三音合同）；Ruff和编译通过。fresh复审尚未完成。
