# 当前代码固定集重建与差异

在独立目录api/tests/fixtures/audio/candidates/closeout-review-20260919-v1使用当前Basic Pitch生产代码完整重跑16条结构集和10条真实钢琴集。原始输入复制，旧评审与旧产物未覆盖；新human/structure-review均pending且reviewer为空。新评审指纹经当前代码重新计算验证一致，不继承旧评级。

## 实测差异

- 26/26原始MIDI音高、起止及力度一致。
- 25/26记谱MIDI及music21解析的分谱音高、时值、起音、休止内容一致。
- 唯一变化为结构集14-hand-crossing，记谱事件264→239，对应此前已实现的25条音尾清理。不是重新换模型或新增本轮删音。
- 16条结构MusicXML的music21、xmllint及OSMD验证通过。真实集由原生成流程的music21解析验证；没有据此声明全部真实集浏览器视觉通过。

这些相等判断不包含音色、视觉排版等全部属性；不同XML导出时间戳不会被误称为音乐变化，语义相等也不会自动迁移评级。

## 收尾决策

正式旧门禁仍有两个过期指纹失败，本轮提供了可追溯的新产物及新绑定，而不是直接把旧评审hash改成当前值。技术复查优先聚焦14变更，既有完整人工评审是否可以接受25条未变内容的证据仍需明确确认；没有收到新审阅前保持pending。06新三音/07/10已接受候选是另一条候选链，不能据此覆盖旧16条结构集的评级。

3项新增测试通过，覆盖失败只落到新目录、禁止覆盖、XML内容变化比较，以及实际26条新绑定/原评级未动/差异集合核验；Ruff/compileall通过。fresh独立审查仍未完成。不创建最终ZIP、不部署、不启动新模型实验。

复现命令（新目录必须不存在）：

```sh
cd songdance/api
.venv/bin/python -m scripts.rebuild_review_isolated --output <new-directory>
.venv/bin/python -m scripts.compare_rebuilt_reviews <new-directory>
```

## 14逐音复查补充：不仅是删25音

后续逐事件对比确认，旧264事件与新239事件的完整(音高/起止/力度)键没有保持为简单子集。新版同时使用固定八分网格和60 BPM，所有音符0.5秒；旧版时值约0.126–1.149秒。此前“对应音尾25条清理”的表述只解释数量变化，不完整解释记谱变化，以此处为准。

50ms起音评价：旧86匹配/178额外/25未匹配，新111匹配/128额外/0未匹配。100ms评价：旧111/153/0，新111/128/0。因此旧版50ms的25处未匹配包含时间偏移，不应直接称25个物理漏音。参考去同刻同键后共111起音。新版休止符85→3，每谱表每小节最大声部右3→1、左2→1。原模型raw.mid不变，音尾删除25项在timeline.crossing_tail_cleanup.removals逐一留档。

结论：节奏和谱表有可验证改善，128额外事件仍是明确质量缺口，14不可直接评级通过。4项针对差异重算/重建隔离测试通过，未新调参、未改原谱、未新增生产删音。完整原始比较见closeout-review-20260919-v1/14-change-audit.json。
