# 本轮收尾结论（2026-09-22）

本轮停止扩展实验，保留Basic Pitch，不部署。用户确认的06新三音、07、10节奏候选原样冻结，不能用它们代表任意上传已达到相同效果。

## 可以关闭

1. 拍格上中位数外推导致稳定节拍偏移：校准BPM与网格使用同一拟合，保留原值审计，限定独立复审通过；小幅速度变化可能在容差内，不承诺全部rubato弃权。
2. 同刻音互相截为零时值：改为下一严格更晚起音，短音/同音/乱序/尾音回归通过，两次限定审查确认无新增必须立即修复的逻辑错误。
3. 配置单测访问真实Redis、07历史意见字段引用及人评单测前提耦合：已修并保留真正过期评审拒绝门禁。
4. 本地浏览器/PDF/音频对照材料验证环境：解除测试环境限制后可运行。14独立材料能播放/下载，长页PDF无裁切，不是音质通过。

## 最新证据（不混用旧绿色结果）

- API最终全量：825 passed / 7 failed / 6 skipped，118.54秒，日志/tmp/songdance-final-20260922-api.log。
- 7失败均当前代码与旧报告/评审绑定不同：四组自动实验报告、当前固定集重建绑定、两份正式人评。零时值修复再次改变score.py，因此前一轮822/2不能当作最新结果。
- 零时值修复相关11文件189 passed（含OSMD/转录服务）；独立定向6 passed。
- Web最新140 tests passed，tsc退出0。Webpack构建与26项端到端此前已通过；本轮未声称再次执行构建/E2E。
- git diff --check通过。工作区仍有大量既有变更，未整体提交，也未把范围外代码声称已审查。

## 保留的质量缺口

| 项目 | 事实 | 状态 |
|---|---|---|
| 08固定集 | 94个记谱事件，53匹配/41额外/6未匹配，13和弦/双谱表 | 音质未通过，原始111事件未变 |
| 14固定集 | 111参考起音保留，128额外事件 | 音质未通过 |
| 10生产Basic | 拍格修复后低音29/30匹配、49额外，非全部恢复 | 音质未通过 |
| Transkun 10候选 | 原模型漏0/15/16秒C2，与Basic缺陷不同 | 离线研究，不切生产 |
| 正式人评 | 旧评级不绑定最终流水线，不能自动迁移 | 两项质量门禁不放行 |

08特殊分支需要eighth_note_melody显式上下文；普通API上传不传这个提示。不能把固定样本候选等同通用产品行为。score.py文件过长属既有维护债，本轮不扩大重构。

## 明确的收尾边界

工程缺陷修复完成上述限定闭环，但整项目尚未全量绿色，转录质量也未全部达标。后续只应在冻结最终代码后统一刷新受影响自动证据，并依据新产物处理正式复评；不再在“刷新→加规则→又失效”之间无限循环。音质优化是独立工作，不能通过删除参考外音符、隐藏失败或改评级伪装完成。

## 冻结后最终刷新结果（取代上方825/7统计）

90个app源码文件已记录于2026-09-22-frozen-code.json，本轮结束核验全部未变。四组自动报告旧目录完整归档为*-before-final-20260922，然后真实重跑；26条新固定集重建至closeout-review-20260922-final，旧版本均保留。对应测试读取新版本，未删除指纹断言。

最新全量API：830 passed / 2 failed / 6 skipped / 0 errors，114.86秒，日志/tmp/songdance-frozen-final-tests.log。自动证据绑定相关50项通过。两个剩余失败仍为正式旧human-review和structure-review与当前流水线指纹不一致，新集10+16条全部pending，没有继承评级。它们只能经真实复评或明确的评审有效性确认关闭，不允许直接重写旧hash。

因此本轮自动证据过期问题已关闭，不再改算法触发下一轮刷新。先前各限定代码复审仍适用；本次新增仅测试当前目录引用。08/14误音、10质量限制仍未解决，不因830项通过消除。没有部署、换模型或生成最终发送ZIP。
