# 15、16 评审核对与后续修复边界

本批反馈已保存原文及结构化条款，新增 AC-115、AC-116。没有收到新的正式评级档位或具体导出文件标识，两条保持需要修订；本次完成反馈和来源核对，不宣称谱面已修复。

## 来源证据

独立检查本地22个ZIP、3个解包目录、原生成文件与3个重建快照：15、16各29份WAV分别同一SHA-256，但曲谱导出版本不同。因此可以按唯一已知音源诊断，不能直接断定老师评审的是最新曲谱。审计脚本重新生成这两段音频并逐PCM16样本比较，一致后才引用生成器事件解释输入。原音频、原始模型MIDI、既有曲谱、原评级均未修改。

| 样本 | 原始来源事件 | Basic Pitch匹配起音（100ms） | 额外事件 | 当前判断 |
|---|---:|---:|---:|---|
| 15-repeated-notes | 118 | 118 | 119 | 真实重击均检出，多认音及记谱/分手问题仍需修复 |
| 16-noisy-polyphony | 120 | 120 | 29 | 已知源内和弦起音均检出；源和弦与三音参考冲突，仍需复核下游呈现 |

这些是相对本地源的起音统计，忽略结束时间；不是“准确谱面”或对教师听感的否定。完整双容差和两份候选曲谱的时值类型统计见隔离审计JSON。

## 15

源为C4 C4 D4 D4 E4 E4 G4 G4循环，0.25秒一个起音；118音包含14个完整八音循环和末尾6音，不应补音填满第15次循环。按八分音符记法对应约120四分BPM；既有曲谱标117，老师认可速度和4/4，本次只记录认可，不臆造其指定数值或改成200多BPM。

修复验收应同时保留每次独立同音起音、以首循环原点量化、不累积小节偏移，分手采用教师“左手前4音/右手后4音”的参考来源。识别出的高音不能仅因为不在11223355模板里就删掉，真八度与真实弱重击必须作为保护反例。这是待实现/验证的规则要求，不是本轮已应用的滤音结果。

## 16

右手十二音为C5 D5 E5 G5 / E5 D5 C5 D5 / E5 G5 E5 D5，与老师三组四音描述一致。每组右手四音占2秒，因此在2/4、每音八分音符的展示约定下，对应四分BPM60、左手和弦两拍。三小节循环可以成立，不能把乐句小节数非1/2/4当通用错误。

本地源左手实际为C3-G3-C4-E4 / F2-A3-C4-F4 / G2-G3-B3-D4，每组4音；与保留源内上方三音的教师三和弦参考相比，多出的低音是C3/F2/G2，各5次。它们来自源演奏事件，并非模型凭空产生的泛音。生成器左手有56个事件持续2.8秒，跨越2秒和弦间隔；末组4个事件因30秒结尾裁为2.0秒。老师要求的每和弦两拍在上述约定下为2秒。这两层差异需要明确处理，不能静默删源内音或把新合成音频当旧音频修复。

此外，pretty_midi读取该参考MIDI时，有9个左手共享同键事件由原生成时的2.8秒变成0.8秒（同键重叠时前一个note_off被用于截断）。音高起音数仍为120。审计同时保留生成器事件和解析后的MIDI事件，避免把解析时值直接当作波形真值。此处只是识别到来源/解析约定差异，并未修复MIDI库或全局改变读谱方式。

## 本轮交付与下一步

- 原文：2026-09-22-teacher-15-16-verbatim.md。
- 反馈条款与源/候选哈希：api/tests/fixtures/audio/teacher-review-15-16.json。
- 审计命令：在api目录运行 `.venv/bin/python -m scripts.audit_teacher_reviews_15_16 --output <new-directory>`。
- 当前审计：api/tests/fixtures/audio/candidates/teacher-15-16-intake-v2/audit.json；v1保留为此前诊断草案，新版从教师条款计算冲突，不恒定返回冲突状态。

先修15的循环对齐与重复击键保护；16先完成源内四音与三音记谱要求的对齐，再处理2/4和弦强拍及分手。根据右手推测的缺和弦音仅可作为待确认建议，不进入“已识别音符”。普通上传生产路径未改，Basic Pitch保持，未训练模型、未生成新的替代音频、未打包/部署。

## 验证与独立审查

运行 `pytest -q tests/test_teacher_reviews_15_16.py`：11 passed；Ruff、编译及diff检查通过。独立code-reviewer两阶段限定审查通过，无HIGH/MEDIUM；提出的末组时值裁切文字精度问题已补明。审查者完整重算v2报告，结果逐字段相同。此结论仅验收本轮反馈录入、源版本诊断及测试，不代表15/16曲谱已经修复或人工复评通过。
