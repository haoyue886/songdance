# 15同音音尾规则迁移核验

在15原始MIDI时间上重新测量基频起音、衰减拟合及频率分离瞬态证据，复用现有crossing-tail-evidence-v2，不改阈值。quarter_seconds=.5来自前一步模型起音拟合120 BPM，仅离线试用，不给15配置交叉手身份或接入生产。

实际输入237事件，规则选中0删除；参考只在规则后评价，118匹配/119额外/0漏保持。原事件及15时间候选不改，未写出删音后的曲谱。额外高音的泛音归属与同音余响均仍未安全解决。不能为删够119音放宽阈值或套11223355模板。

证据目录api/tests/fixtures/audio/candidates/15-tail-audit-v1，含原输入绑定、音频预处理哈希、6份源码哈希、完整音频测量和每条规则决策。参考起音匹配忽略结束时间，不代表音符时值正确。confidence为MIDI velocity/127占位，并非模型校准概率。

首次审计测试对before/after全字典相等断言失败，原因是after使用NoteEvent增加confidence/hand字段，非音乐事件变化；改为分别按各自真实结构完整重算，并保留kept==events逐项一致断言及匹配计数一致检查。没有修改报告来消除失败。

相关129测试通过（39.86秒），Ruff/编译/diff通过。独立审查完整重生成报告与存档一致；测试比较问题修复后审查者重读重跑1项通过，限定无HIGH/MEDIUM。修复后fresh新任务受thread limit阻塞，最终单测试复核由原只读审查任务执行，非新的独立全量审查。

## 起音证据可辨识性核对

逐事件精确绑定同音高/原始起止与onset_observations，参考标签仅作事后诊断：118个正确起音中72被标independent_onset=true、46为false；119个额外事件中65为true、54为false。瞬态值可测数量分别30和23，不能把缺失测量当成可删。

这不是单纯“阈值过保守”：快速重复音和同时泛音会使现有起音标记产生混淆，false不代表未按键、true不保证真实独立琴键。按此标记直接过滤会误删46个真实起音；当前清理规则因为还有额外保护条件，实际删除仍为0。

逐事件映射和输入SHA保存15-tail-audit-v1/onset-diagnostic.json。下一步需要更可靠的起音证据或人工编辑路径，而不是继续调宽泛音/尾音阈值。15暂保留为失败样本；时间对齐完成不等于误音解决，不能为上线把它改成通过。
