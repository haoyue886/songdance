# 15重复音：时间候选v2

本轮只实现隔离时间对齐，不做泛音删除、分手或正式曲谱签收。输入为绑定的15原Basic Pitch MIDI，全部237个事件保留，音高/力度不变；参考MIDI仅在输出后用于评价。

模型起音按80ms窗口分簇，119个簇的中位相邻间隔与直线拟合支持约0.25秒八分单位；拟合间隔0.249976秒，记谱120四分BPM，保持教师4/4八分约定，不机械翻倍。使用首簇0.022727秒为统一音频网格原点，所有事件映射槽号并统一0.25秒时值，最大起音调整61.4ms；超过准入容差或同键同槽碰撞拒绝，不合并真实重击。

首版v1的MIDI仍使用音频绝对起音，造成MIDI首拍0与首音22.7ms不一致。v2导出减原点，首音在MIDI0秒；audit记录audio_seconds=midi_seconds+0.022727。直接MIDI播放相对原音需使用该偏移，不能宣称未经对齐就天然同步。v1保留但不作为交付版本。

v2全部237音保留，音频时间轴50/100ms评价118正确起音/119额外/0漏。源118音为14完整循环+6尾音，模型119起音簇多出1簇；本轮未根据真值删除最后簇或补音填小节。分手暂不应用，避免把未清理额外高音编成教师已确认演奏。

17项相关测试通过，含重复同键、同时高音保留、同键碰撞拒绝、变速拒绝、真输出重算，以及现场run导出后逐音核对时值/力度、120BPM/4拍、MIDI首音0和八槽小节相位。Ruff/compileall/diff通过。输出：api/tests/fixtures/audio/candidates/15-timing-only-v2/candidate.mid和audit.json。

这是针对老师确认的15规则性循环的显式候选，不自动启用到所有上传。119额外事件未解决，未生成可用谱面、未切换模型、不部署。独立复审结果另行记录。

修复后fresh两阶段复审通过：原2项MEDIUM关闭，本范围0HIGH/0MEDIUM。独立现场6测试、Ruff和编译通过；v2实际MIDI首音0、下拍0/2/4秒，输入与产物哈希一致。此结论不解除泛音/分手/正式听审未完成状态。
