# 16 额外事件与音尾证据

新增 `scripts/audit_polyphony_extras.py`，按 DEV-PLAN 专项执行。正式本轮输出为 `tests/fixtures/audio/candidates/16-extra-audit-v2`；v1保留格式化前代码指纹，不覆盖。

先在绑定原音频提取泛音、起音、衰减和瞬态证据，运行现有泛音和 crossing-tail 判据。随后才读取经逐PCM重建核验的生成器事件，进行事后匹配。使用生成器结束时间，避免原始参考MIDI同键重叠解析导致的错误时值。60 BPM 是既定记谱假设，confidence=0是未知占位，不代表模型概率。

实测149模型事件中120匹配源起音，29额外（100ms，仅起音匹配）。现有两类规则均选0删除；实际应用删除0。

29额外事件中12在先前同音高源音持续范围内。这只是音尾再触发嫌疑，不能直接当删除许可。其余17也不能由此统称泛音。

独立起音标记：120匹配事件中115 true、5 false；29额外中14 true、15 false。可见简单地“无独立起音就删除”会误删真实音，并留下额外事件。停止使用该布尔值作为单独删除依据，不通过调宽阈值或按教师音高模板强删。

验证：45项相关测试通过，追加不可用音频证据失败留档测试后专项4项通过；Ruff、编译及v2所有记录的代码指纹核对通过。未跑全量API或Web。独立审查派发返回 `unsupported call: spawn_agent`，因此本轮没有独立审查通过结论。

本轮交付是可复跑的逐事件证据工具，不是音质修复。生产模型、谱面和人评评级不变，无最终评审包。下一项算法工作应提供比单一起音标记更强的跨时间频谱证据，至少同时验证这12个疑似再触发与5个弱起音保护反例，再考虑隔离候选；在独立验证前不接入生产。

## v4：纠正开头起音解释与逐项失败原因

上一节“5个真实起音”计数正确，但将其理解成弱音漏判不成立：5个事件均位于约0.01136秒，原判据向前80ms的采样点被钳制到0秒，缺乏完整的起音前对照。本轮不将false改为true，也不改变生产判断；新增 `pre_onset_probe_clamped` 仅标记观测边界（不代表完整STFT窗口可用性）。真正的弱击键反例须另外构造，不能用这5音冒充。

新增 `polyphony_evidence_diagnostics.py` 为全部149事件解释现有局部衰减必要条件，不提供删除许可，不复刻全套前驱/根起音/链深判据。12个同音持续覆盖的额外事件中：9个衰减拟合误差超标，6个瞬态拟合误差超标，6个瞬态证据不可用；这些计数有重叠。12个均未通过局部必要条件。

v3真实执行发现numpy bool_不可JSON序列化，已将边界标记显式转换为Python bool；v3 failed留档，v4现场重跑成功，代码指纹核对一致。55项相关测试通过，Ruff与编译通过。实际删音仍为0，不产生曲谱v5，不改变人工评级。

范围结论：本轮完成证据解释和错误诊断工具，未实现新的跨时间频谱分离算法，也未解决29额外音。单阈值扩张路径停止；后续若开展算法实验，必须包含真实弱重复击键及泛音重叠的独立控制，不依照这12音的标签调参。

独立两阶段限定审查已通过：0 HIGH / 0 MEDIUM / 0 LOW；审查者独立22项测试通过、真实重跑149项局部解释全部与生产判据一致，并确认v2/v4既有事件及选择无变化、v4指纹全部匹配。此结论仅覆盖证据解释工具，不表示16音乐质量通过。
