# 真人听辨本地页面

为real-repeat-annotations-v2增加index.html，原120条labels仍pending，原片段/manifest未改。可直接file打开、播放WAV、显示候选位置、逐条填写/导航、按manifest缓存、导入导出草稿。无需Python服务，无外部请求，无自动真值或删音，无最终ZIP。

生成器检查manifest/片段绑定，JSON内嵌转义防脚本注入、拒绝覆盖已有HTML。页面内使用textContent呈现字段，导入校验身份/枚举/数字/时点并确认替换。完整性仍由既有Python validator校验，页面仅允许导出待完成草稿。

独立审查发现并修复两项MEDIUM：非法音高污染state导致后续标注无法保存，现字段错误回滚并阻止导航；随后发现缓存配额故障不应阻止合法数据导出，现分离字段校验与缓存写入，缓存失败保留内存并提醒立即下载，不阻断导航/导出。

主Python相关8测试通过，Ruff/编译通过。真实隔离Chrome file测试覆盖播放推进、导航保存、刷新恢复、导入导出、错误导入、非法音高导航阻断、缓存满额下载及导航、375px无横向溢出；缓存测试使用独立浏览器上下文，未写实际标注。目视检查手机截图并对比已有14-closeout本地页面，保留朴素字体/细边框分区结构，无遮挡截断。无需与旧页控件完全相同（本页为新增标注表单）。

最新fresh限定两阶段复审通过：M1/M2关闭，0HIGH/0MEDIUM。独立VM注入缓存故障验证状态保留/下载/导航，非法音高仍阻断；独立查看桌面基准与新页截图，未见截断。主完整Chrome故障路径已通过。以上不表示获得人工真值或音乐质量通过。
